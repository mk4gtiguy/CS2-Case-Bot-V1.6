CS2 Case Bot V1.6 — Complete Code Reference
Created by Mk4gtiguy | For Streamer.bot v1.0.4+

TABLE OF CONTENTS
New Persisted Globals

Economy Values

Core Actions

Case Actions

Capsule & Sticker Actions

Economy Actions

Quest Actions

Leaderboard Actions

Collection Actions

Lottery Actions

Referral Actions

Giveaway Actions

Jackpot Actions

Trading Actions

Stats Actions

Admin Actions

Help Action

Timer Setup Instructions

NEW PERSISTED GLOBALS
CS20 Case (Add these 5)
text
CS20GoldList = Classic Knife | Fade;Classic Knife | Crimson Web;Classic Knife | Case Hardened;Classic Knife | Slaughter;Classic Knife | Doppler;Classic Knife | Tiger Tooth;Classic Knife | Marble Fade;Classic Knife | Lore;Classic Knife | Night;Classic Knife | Blue Steel;Classic Knife | Stained;Classic Knife | Urban Masked;Classic Knife | Forest DDPAT
CS20RedList = AWP | Wildfire;FAMAS | Commemoration
CS20PinkList = MP9 | Hydra;AUG | Death by Puppy;P90 | Nostalgia
CS20PurpleList = MP5-SD | Agent;UMP-45 | Plastique;P250 | Inferno;Five-SeveN | Buddy;M249 | Aztec
CS20BlueList = Glock-18 | Sacrifice;FAMAS | Decommissioned;Dual Berettas | Elite 1.6;MAC-10 | Classic Crate;MAG-7 | Popdog;Tec-9 | Flash Out;SCAR-20 | Assault
Sticker Capsules (Add all 25 - NO EMOJIS IN NAMES)
Recoil Sticker Capsule

text
RecoilStickerBlueList = Natus Vincere;Fnatic;Team Liquid;Astralis;NAVI;FaZe;Cloud9;Virtus pro;G2;ENCE
RecoilStickerPurpleList = s1mple;dev1ce;ZywOo;Niko;electronic;Twistzz;ropz;blameF
RecoilStickerPinkList = Crown;Howling Dawn;Team Spirit;Flammable
RecoilStickerRedList = iBuyPower;Titan;Katowice 2014
RecoilStickerGoldList = Gold ESL Skull;Gold Crown;Gold Skull
Dreams Sticker Capsule

text
DreamsStickerBlueList = DreamHack;ESL;BLAST;IEM;Faceit;ESEA;Fragbite;PGL
DreamsStickerPurpleList = KennyS;olofmeister;GeT_RiGhT;f0rest;coldzera;fallen
DreamsStickerPinkList = Phoenix;Dragon;Lion;Eagle
DreamsStickerRedList = LGB;Vox Eminor;Reason
DreamsStickerGoldList = Gold Phoenix;Gold Dragon;Gold Eagle
Championship Capsule

text
ChampionshipStickerBlueList = Champion;Victory;Trophy;Winner;Elite;Premier
ChampionshipStickerPurpleList = MVP;Ace;Clutch;1v5;Ninja
ChampionshipStickerPinkList = Diamond;Platinum;Emerald;Ruby
ChampionshipStickerRedList = Championship;Grand Slam;Triple Crown
ChampionshipStickerGoldList = Gold Champion;Gold Trophy;Gold Medal
Legends Capsule

text
LegendsStickerBlueList = Legend;Mythic;Fabled;Epic;Hero;Icon
LegendsStickerPurpleList = Hall of Fame;Immortal;Eternal;Infinite
LegendsStickerPinkList = Celestial;Nebula;Galaxy;Cosmos
LegendsStickerRedList = Ancient;Primordial;First Blood
LegendsStickerGoldList = Gold Legend;Gold Mythic;Gold Eternal
CS20 Sticker Capsule

text
CS20StickerBlueList = 20 Years;Anniversary;Classic;Retro;Vintage;Legacy
CS20StickerPurpleList = Milestone;Decades;OG;Pioneer
CS20StickerPinkList = Commemorative;Collector;Limited
CS20StickerRedList = CS20;Two Decades;Legendary
CS20StickerGoldList = Gold CS20;Gold Anniversary;Gold Legacy
ECONOMY VALUES
Weapon Values
Rarity	Value
Blue (Mil-Spec)	$0.25
Purple (Restricted)	$1.00
Pink (Classified)	$4.00
Red (Covert)	$20.00
Gold Weapon Tiers (within 2.6% Gold chance)
Tier	Value	Chance within Gold
Common	$150	50%
Rare	$300	30%
Epic	$600	15%
Legendary	$1000	4.5%
Mythic	$2500	0.5%
Sticker Values
Rarity	Value
⭐ (High Grade)	$0.10
✨ (Remarkable)	$0.50
💫 (Exotic)	$2.00
🔥 (Extraordinary)	$10.00
Gold Sticker Tiers (within 👑)
Tier	Value	Chance
Common	$30	50%
Rare	$75	30%
Epic	$150	15%
Legendary	$300	5%
Case Costs
Case	Cost
Recoil	$0.50
Dreams, Revolution, CS20	$1.50
Kilowatt, Fracture, Horizon, Chroma3	$2.00
Prisma, DangerZone, Spectrum2	$2.50
Clutch	$3.00
Capsule Costs
Capsule	Cost
Recoil	$0.50
Dreams, CS20	$1.00
Championship	$2.00
Legends	$3.00
Streak Bonuses
Streak	Bonus
10	$25
25	$75
50	$250
100	$1000
CORE ACTIONS
ACTION: CS2_OpenCase (V1.6)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        var random = new Random();
        
        string user = CPH.GetGlobalVar<string>("User");
        string currentCase = CPH.GetGlobalVar<string>("CurrentCase");
        string casePrefix = currentCase.Replace(" ", "").Replace("&", "").Replace("-", "");
        
        // Update stats tracking
        int dailyOpens = CPH.GetGlobalVar<int>("Stats_DailyOpens");
        dailyOpens++;
        CPH.SetGlobalVar("Stats_DailyOpens", dailyOpens, true);
        
        int weeklyOpens = CPH.GetGlobalVar<int>("Stats_WeeklyOpens");
        weeklyOpens++;
        CPH.SetGlobalVar("Stats_WeeklyOpens", weeklyOpens, true);
        
        int monthlyOpens = CPH.GetGlobalVar<int>("Stats_MonthlyOpens");
        monthlyOpens++;
        CPH.SetGlobalVar("Stats_MonthlyOpens", monthlyOpens, true);
        
        // Update quest progress
        string quests = CPH.GetGlobalVar<string>(user + "_Quests");
        if (!string.IsNullOrEmpty(quests))
        {
            string[] parts = quests.Split(';');
            for (int i = 0; i < parts.Length; i++)
            {
                if (parts[i].StartsWith("Daily_3cases:"))
                {
                    int val = int.Parse(parts[i].Substring(13)) + 1;
                    parts[i] = "Daily_3cases:" + val;
                }
                if (parts[i].StartsWith("Daily_specific:"))
                {
                    string dailySpecificCase = CPH.GetGlobalVar<string>(user + "_DailySpecificCase");
                    if (currentCase == dailySpecificCase)
                    {
                        int val = int.Parse(parts[i].Substring(15)) + 1;
                        parts[i] = "Daily_specific:" + val;
                    }
                }
            }
            quests = string.Join(";", parts);
            CPH.SetGlobalVar(user + "_Quests", quests, true);
        }
        
        // Streak tracking
        int currentStreak = CPH.GetGlobalVar<int>(user + "_OpenStreak");
        currentStreak++;
        CPH.SetGlobalVar(user + "_OpenStreak", currentStreak, true);
        
        // Streak bonuses
        if (currentStreak == 10 || currentStreak == 25 || currentStreak == 50 || currentStreak == 100)
        {
            double bonus = 0;
            if (currentStreak == 10) bonus = 25.00;
            else if (currentStreak == 25) bonus = 75.00;
            else if (currentStreak == 50) bonus = 250.00;
            else if (currentStreak == 100) bonus = 1000.00;
            
            double balance = CPH.GetGlobalVar<double>(user + "_Balance");
            balance += bonus;
            CPH.SetGlobalVar(user + "_Balance", balance, true);
            CPH.SendMessage("🔥 " + user + " reached a " + currentStreak + " open streak! Bonus $" + bonus.ToString("F2") + " added!");
        }
        
        // Roll rarity
        int roll = random.Next(1, 1001);
        string rarityType;
        if (roll <= 26) rarityType = "Gold";
        else if (roll >= 975) rarityType = "Red";
        else if (roll >= 950) rarityType = "Pink";
        else if (roll >= 900) rarityType = "Purple";
        else rarityType = "Blue";
        
        // Base values
        string emoji;
        double baseValue;
        if (rarityType == "Gold") { emoji = "🟡"; baseValue = 150.00; }
        else if (rarityType == "Red") { emoji = "🔴"; baseValue = 20.00; }
        else if (rarityType == "Pink") { emoji = "💗"; baseValue = 4.00; }
        else if (rarityType == "Purple") { emoji = "🟣"; baseValue = 1.00; }
        else { emoji = "🔷"; baseValue = 0.25; }
        
        // Get skin list
        string skinList = CPH.GetGlobalVar<string>(casePrefix + rarityType + "List");
        if (string.IsNullOrEmpty(skinList)) skinList = CPH.GetGlobalVar<string>(rarityType + "List");
        
        var validSkins = new List<string>();
        foreach (string skin in skinList.Split(';'))
            if (!string.IsNullOrWhiteSpace(skin))
                validSkins.Add(skin.Trim());
        
        string currentSkin = validSkins[random.Next(0, validSkins.Count)];
        
        // Apply condition
        string[] conditions = { "Factory New", "Minimal Wear", "Field-Tested", "Well-Worn", "Battle-Scarred" };
        double[] conditionMultipliers = { 2.0, 1.5, 1.0, 0.75, 0.5 };
        int conditionRoll = random.Next(1, 101);
        int conditionIndex = 0;
        if (conditionRoll <= 5) conditionIndex = 0;
        else if (conditionRoll <= 20) conditionIndex = 1;
        else if (conditionRoll <= 70) conditionIndex = 2;
        else if (conditionRoll <= 90) conditionIndex = 3;
        else conditionIndex = 4;
        
        string condition = conditions[conditionIndex];
        double finalValue = baseValue * conditionMultipliers[conditionIndex];
        
        // Apply StatTrak
        bool isStatTrak = random.Next(1, 101) <= 10;
        if (isStatTrak) finalValue *= 2;
        
        // Apply Gold tier
        string goldTierTag = "";
        if (rarityType == "Gold")
        {
            int goldTierRoll = random.Next(1, 1001);
            if (goldTierRoll <= 500) { finalValue = 150.00; goldTierTag = " [Common]"; }
            else if (goldTierRoll <= 800) { finalValue = 300.00; goldTierTag = " [Rare]"; }
            else if (goldTierRoll <= 950) { finalValue = 600.00; goldTierTag = " [Epic]"; }
            else if (goldTierRoll <= 995) { finalValue = 1000.00; goldTierTag = " [Legendary]"; }
            else { finalValue = 2500.00; goldTierTag = " [Mythic]"; }
        }
        
        string statTrakTag = isStatTrak ? " (StatTrak™)" : "";
        string newItem = emoji + " " + currentSkin + " (" + condition + ")" + goldTierTag + statTrakTag;
        
        // Save to inventory
        string oldItems = CPH.GetGlobalVar<string>(user + "_Items");
        string newItemsList = string.IsNullOrEmpty(oldItems) ? newItem : oldItems + ";" + newItem;
        CPH.SetGlobalVar(user + "_Items", newItemsList, true);
        
        // Track case opens
        string caseKey = casePrefix + "_Opened";
        int caseOpened = CPH.GetGlobalVar<int>(caseKey);
        CPH.SetGlobalVar(caseKey, caseOpened + 1, true);
        
        int userCaseOpened = CPH.GetGlobalVar<int>(user + "_" + casePrefix + "_Opened");
        CPH.SetGlobalVar(user + "_" + casePrefix + "_Opened", userCaseOpened + 1, true);
        
        int totalOpened = CPH.GetGlobalVar<int>(user + "_TotalOpened");
        CPH.SetGlobalVar(user + "_TotalOpened", totalOpened + 1, true);
        
        // Case unlocks
        string unlockedCases = CPH.GetGlobalVar<string>(user + "_UnlockedCases");
        if (string.IsNullOrEmpty(unlockedCases)) unlockedCases = "Dreams;Recoil";
        
        int totalCasesOpened = totalOpened + 1;
        if (totalCasesOpened >= 25 && !unlockedCases.Contains("Kilowatt"))
        { unlockedCases += ";Kilowatt"; CPH.SendMessage("🔓 " + user + " unlocked the Kilowatt case!"); }
        if (totalCasesOpened >= 50 && !unlockedCases.Contains("Revolution"))
        { unlockedCases += ";Revolution"; CPH.SendMessage("🔓 " + user + " unlocked the Revolution case!"); }
        if (totalCasesOpened >= 75 && !unlockedCases.Contains("Fracture"))
        { unlockedCases += ";Fracture"; CPH.SendMessage("🔓 " + user + " unlocked the Fracture case!"); }
        if (totalCasesOpened >= 100 && !unlockedCases.Contains("Clutch"))
        { unlockedCases += ";Clutch"; CPH.SendMessage("🔓 " + user + " unlocked the Clutch case!"); }
        if (totalCasesOpened >= 150 && !unlockedCases.Contains("Prisma"))
        { unlockedCases += ";Prisma"; CPH.SendMessage("🔓 " + user + " unlocked the Prisma case!"); }
        if (totalCasesOpened >= 200 && !unlockedCases.Contains("DangerZone"))
        { unlockedCases += ";DangerZone"; CPH.SendMessage("🔓 " + user + " unlocked the Danger Zone case!"); }
        CPH.SetGlobalVar(user + "_UnlockedCases", unlockedCases, true);
        
        // Track rarity counts
        string rarityCounterKey = user + "_" + rarityType + "s";
        int rarityCount = CPH.GetGlobalVar<int>(rarityCounterKey);
        CPH.SetGlobalVar(rarityCounterKey, rarityCount + 1, true);
        
        // Achievements
        string achievements = CPH.GetGlobalVar<string>(user + "_Achievements");
        if (string.IsNullOrEmpty(achievements)) achievements = "";
        
        if (rarityType == "Gold" && !achievements.Contains("FirstGold"))
        { achievements += (achievements == "" ? "" : ";") + "FirstGold"; CPH.SendMessage("🏆 " + user + " earned 'First Gold'!"); }
        
        int tradeupCount = CPH.GetGlobalVar<int>(user + "_TradeupCount");
        if (tradeupCount >= 10 && !achievements.Contains("TradeUpMaster"))
        { achievements += (achievements == "" ? "" : ";") + "TradeUpMaster"; CPH.SendMessage("🏆 " + user + " earned 'Trade Up Master'!"); }
        
        int jackpotWins = CPH.GetGlobalVar<int>(user + "_JackpotWins");
        if (jackpotWins >= 1 && !achievements.Contains("JackpotWinner"))
        { achievements += (achievements == "" ? "" : ";") + "JackpotWinner"; CPH.SendMessage("🏆 " + user + " earned 'Jackpot Winner'!"); }
        
        CPH.SetGlobalVar(user + "_Achievements", achievements, true);
        
        // Gold tracking
        if (rarityType == "Gold")
        {
            int userGolds = CPH.GetGlobalVar<int>(user + "_Golds");
            CPH.SetGlobalVar(user + "_Golds", userGolds + 1, true);
            int totalGolds = CPH.GetGlobalVar<int>("GoldPulls_Total");
            CPH.SetGlobalVar("GoldPulls_Total", totalGolds + 1, true);
        }
        
        // Track all users
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        if (string.IsNullOrEmpty(allUsers)) allUsers = user;
        else
        {
            bool userExists = false;
            foreach (string existingUser in allUsers.Split('§'))
                if (existingUser.Equals(user, StringComparison.OrdinalIgnoreCase))
                { userExists = true; break; }
            if (!userExists) allUsers += "§" + user;
        }
        CPH.SetGlobalVar("AllUsers", allUsers, true);
        
        CPH.SetGlobalVar("CurrentSkin", currentSkin, true);
        CPH.SetGlobalVar("RarityType", rarityType, true);
        CPH.SetGlobalVar("Emoji", emoji, true);
        CPH.SetGlobalVar("Value", finalValue.ToString("F2"), true);
        CPH.SetGlobalVar("CurrentValue", finalValue, true);
        
        System.Threading.Thread.Sleep(2500);
        
        CPH.SendMessage("🎁 " + user + " opened " + currentCase + " and got: " + newItem + " (Value: $" + finalValue.ToString("F2") + ")");
        return true;
    }
}
ACTION: CS2_OpenCapsule (V1.6)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        var random = new Random();
        
        string user = CPH.GetGlobalVar<string>("User");
        string currentCapsule = CPH.GetGlobalVar<string>("CurrentCapsule");
        
        int roll = random.Next(1, 1001);
        
        string rarity;
        string emoji;
        double value;
        
        if (roll <= 26) { rarity = "Gold"; emoji = "👑"; value = 30.00; }
        else if (roll >= 975) { rarity = "Red"; emoji = "🔥"; value = 10.00; }
        else if (roll >= 950) { rarity = "Pink"; emoji = "💫"; value = 2.00; }
        else if (roll >= 900) { rarity = "Purple"; emoji = "✨"; value = 0.50; }
        else { rarity = "Blue"; emoji = "⭐"; value = 0.10; }
        
        // Apply Gold tier
        string goldTierTag = "";
        if (rarity == "Gold")
        {
            int goldTierRoll = random.Next(1, 1001);
            if (goldTierRoll <= 500) { value = 30.00; goldTierTag = " [Common]"; }
            else if (goldTierRoll <= 800) { value = 75.00; goldTierTag = " [Rare]"; }
            else if (goldTierRoll <= 950) { value = 150.00; goldTierTag = " [Epic]"; }
            else { value = 300.00; goldTierTag = " [Legendary]"; }
        }
        
        string stickerListKey = currentCapsule + "Sticker" + rarity + "List";
        string stickerList = CPH.GetGlobalVar<string>(stickerListKey);
        
        if (string.IsNullOrEmpty(stickerList))
        {
            CPH.SendMessage(user + " error: no sticker list found for " + currentCapsule + " " + rarity + "!");
            return false;
        }
        
        string[] stickers = stickerList.Split(';');
        string sticker = stickers[random.Next(0, stickers.Length)].Trim();
        
        string newSticker = emoji + " " + sticker + goldTierTag;
        
        string oldItems = CPH.GetGlobalVar<string>(user + "_Items");
        string newItemsList = string.IsNullOrEmpty(oldItems) ? newSticker : oldItems + ";" + newSticker;
        CPH.SetGlobalVar(user + "_Items", newItemsList, true);
        
        CPH.SendMessage("📯 " + user + " opened a " + currentCapsule + " capsule and got: " + newSticker + " (Value: $" + value.ToString("F2") + ")");
        return true;
    }
}
CASE ACTIONS
ACTION: SetCase_Command (!setcase)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string caseName = rawInput.Replace("!setcase", "").Trim();
        
        string allCasesList = "Dreams, Recoil, Kilowatt, Revolution, Fracture, Clutch, Prisma, DangerZone, Horizon, Chroma3, Spectrum2, CS20";
        
        if (string.IsNullOrEmpty(caseName))
        {
            CPH.SendMessage("📦 " + user + " Usage: !setcase [case name]\n\nAvailable cases: Dreams, Recoil, Kilowatt, Revolution, Fracture, Clutch, Prisma, DangerZone, Horizon, Chroma3, Spectrum2, CS20\n\nExample: !setcase Dreams");
            return false;
        }
        
        string[] allCases = { "Dreams", "Recoil", "Kilowatt", "Revolution", "Fracture", "Clutch", "Prisma", "DangerZone", "Horizon", "Chroma3", "Spectrum2", "CS20" };
        bool isValid = false;
        
        foreach (string validCase in allCases)
        {
            if (caseName.Equals(validCase, StringComparison.OrdinalIgnoreCase))
            {
                caseName = validCase;
                isValid = true;
                break;
            }
        }
        
        if (!isValid)
        {
            CPH.SendMessage("❌ " + user + " '" + caseName + "' isn't a valid case.\n\nAvailable cases: " + allCasesList + "\n\nExample: !setcase Dreams");
            return false;
        }
        
        string unlockedCases = CPH.GetGlobalVar<string>(user + "_UnlockedCases");
        if (string.IsNullOrEmpty(unlockedCases)) unlockedCases = "Dreams;Recoil";
        
        if (!unlockedCases.Contains(caseName) && caseName != "Dreams" && caseName != "Recoil")
        {
            CPH.SendMessage("🔒 " + user + " " + caseName + " case is locked. Keep opening cases to unlock it!\n\nCurrently unlocked: " + unlockedCases.Replace(";", ", "));
            return false;
        }
        
        CPH.SetGlobalVar(user + "_LastCase", caseName, true);
        
        // Get cost (check for custom price)
        double cost = GetBaseCaseCost(caseName);
        double customPrice = CPH.GetGlobalVar<double>("Price_" + caseName);
        if (customPrice > 0) cost = customPrice;
        
        CPH.SendMessage("📦 " + user + " switched to " + caseName + " case! ($" + cost.ToString("F2") + " per open) | Type !case to open it.");
        return true;
    }
    
    private double GetBaseCaseCost(string caseName)
    {
        if (caseName == "Recoil") return 0.50;
        if (caseName == "Dreams" || caseName == "Revolution" || caseName == "CS20") return 1.50;
        if (caseName == "Kilowatt" || caseName == "Fracture" || caseName == "Horizon" || caseName == "Chroma3") return 2.00;
        if (caseName == "Prisma" || caseName == "DangerZone" || caseName == "Spectrum2") return 2.50;
        if (caseName == "Clutch") return 3.00;
        return 1.50;
    }
}
ACTION: Case_Command (!case)
Must have Sub-Action: Run Action → CS2_OpenCase

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string currentCase = CPH.GetGlobalVar<string>(user + "_LastCase");
        
        if (string.IsNullOrEmpty(currentCase))
        {
            CPH.SendMessage("❌ " + user + " You haven't selected a case yet. Type !setcase Dreams then !case to open it.");
            return false;
        }
        
        CPH.SetGlobalVar("User", user, true);
        CPH.SetGlobalVar("CurrentCase", currentCase, true);
        
        // Get base cost
        double cost = GetBaseCaseCost(currentCase);
        // Check for custom price override
        double customPrice = CPH.GetGlobalVar<double>("Price_" + currentCase);
        if (customPrice > 0) cost = customPrice;
        
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        
        if (balance < cost)
        {
            double shortfall = cost - balance;
            CPH.SendMessage($"❌ {user} You need ${cost:F2} to open a {currentCase} case. You have ${balance:F2}. Type !daily or !sell to get more money.");
            return false;
        }
        
        balance -= cost;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        CPH.SetGlobalVar("CaseCost", cost, true);
        
        CPH.SendMessage($"🔑 {user} bought a {currentCase} key for ${cost:F2} | Balance: ${balance:F2}");
        
        return true;
    }
    
    private double GetBaseCaseCost(string caseName)
    {
        if (caseName == "Recoil") return 0.50;
        if (caseName == "Dreams" || caseName == "Revolution" || caseName == "CS20") return 1.50;
        if (caseName == "Kilowatt" || caseName == "Fracture" || caseName == "Horizon" || caseName == "Chroma3") return 2.00;
        if (caseName == "Prisma" || caseName == "DangerZone" || caseName == "Spectrum2") return 2.50;
        if (caseName == "Clutch") return 3.00;
        return 1.50;
    }
}
ACTION: Gift_Command (!gift) - With Quantity Support
Must have Sub-Action: Run Action → CS2_OpenCase

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string sender = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!gift"))
        {
            int cmdIndex = afterCommand.IndexOf("!gift");
            afterCommand = afterCommand.Substring(cmdIndex + 5).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 1)
        {
            CPH.SendMessage(sender + " Usage: !gift [quantity] @username [case] | Example: !gift 5 @user Dreams");
            return false;
        }
        
        int quantity = 1;
        string target = "";
        string caseName = "Dreams";
        string[] validCases = { "dreams", "recoil", "kilowatt", "revolution", "fracture", "clutch", "prisma", "dangerzone", "horizon", "chroma3", "spectrum2", "cs20" };
        
        int partIndex = 0;
        
        int testQuantity;
        if (int.TryParse(parts[0], out testQuantity))
        {
            quantity = Math.Min(testQuantity, 10);
            partIndex = 1;
        }
        
        if (parts.Length <= partIndex)
        {
            CPH.SendMessage(sender + " Please specify a username!");
            return false;
        }
        
        for (int i = partIndex; i < parts.Length; i++)
        {
            string cleanPart = parts[i].Replace("@", "").Trim().ToLower();
            bool isValidCase = false;
            foreach (string valid in validCases)
            {
                if (cleanPart == valid)
                {
                    isValidCase = true;
                    if (cleanPart == "dreams") caseName = "Dreams";
                    else if (cleanPart == "recoil") caseName = "Recoil";
                    else if (cleanPart == "kilowatt") caseName = "Kilowatt";
                    else if (cleanPart == "revolution") caseName = "Revolution";
                    else if (cleanPart == "fracture") caseName = "Fracture";
                    else if (cleanPart == "clutch") caseName = "Clutch";
                    else if (cleanPart == "prisma") caseName = "Prisma";
                    else if (cleanPart == "dangerzone") caseName = "DangerZone";
                    else if (cleanPart == "horizon") caseName = "Horizon";
                    else if (cleanPart == "chroma3") caseName = "Chroma3";
                    else if (cleanPart == "spectrum2") caseName = "Spectrum2";
                    else if (cleanPart == "cs20") caseName = "CS20";
                    break;
                }
            }
            if (!isValidCase && string.IsNullOrEmpty(target))
            {
                target = cleanPart;
            }
        }
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage(sender + " Please specify a username!");
            return false;
        }
        
        double targetBalance = CPH.GetGlobalVar<double>(target + "_Balance");
        if (targetBalance == 0 && CPH.GetGlobalVar<string>(target + "_Items") == null)
        {
            CPH.SendMessage(sender + " User @" + target + " hasn't played the bot yet!");
            return false;
        }
        
        string unlockedCases = CPH.GetGlobalVar<string>(target + "_UnlockedCases");
        if (string.IsNullOrEmpty(unlockedCases)) unlockedCases = "Dreams;Recoil";
        
        if (!unlockedCases.Contains(caseName) && caseName != "Dreams" && caseName != "Recoil")
        {
            CPH.SendMessage(sender + " @" + target + " hasn't unlocked " + caseName + " yet!");
            return false;
        }
        
        double cost = GetCaseCost(caseName) * quantity;
        
        double senderBalance = CPH.GetGlobalVar<double>(sender + "_Balance");
        if (senderBalance < cost)
        {
            CPH.SendMessage(sender + " You need $" + cost.ToString("F2") + " to gift " + quantity + " " + caseName + " case(s)!");
            return false;
        }
        
        senderBalance -= cost;
        CPH.SetGlobalVar(sender + "_Balance", senderBalance, true);
        
        for (int i = 0; i < quantity; i++)
        {
            CPH.SetGlobalVar("User", target, true);
            CPH.SetGlobalVar("CurrentCase", caseName, true);
            CPH.SetGlobalVar(target + "_LastCase", caseName, true);
            System.Threading.Thread.Sleep(500);
            CPH.RunAction("CS2_OpenCase");
        }
        
        CPH.SendMessage("🎁 " + sender + " gifted " + quantity + " " + caseName + " case(s) to " + target + "!");
        return true;
    }
    
    private double GetCaseCost(string caseName)
    {
        if (caseName == "Recoil") return 0.50;
        if (caseName == "Dreams" || caseName == "Revolution" || caseName == "CS20") return 1.50;
        if (caseName == "Kilowatt" || caseName == "Fracture" || caseName == "Horizon" || caseName == "Chroma3") return 2.00;
        if (caseName == "Prisma" || caseName == "DangerZone" || caseName == "Spectrum2") return 2.50;
        if (caseName == "Clutch") return 3.00;
        return 1.50;
    }
}
ACTION: CaseInfo_Command (!caseinfo) - Shows Custom Prices
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        double dreamsPrice = GetDisplayPrice("Dreams", 1.50);
        double recoilPrice = GetDisplayPrice("Recoil", 0.50);
        double kilowattPrice = GetDisplayPrice("Kilowatt", 2.00);
        double revolutionPrice = GetDisplayPrice("Revolution", 1.50);
        double fracturePrice = GetDisplayPrice("Fracture", 2.00);
        double clutchPrice = GetDisplayPrice("Clutch", 3.00);
        double prismaPrice = GetDisplayPrice("Prisma", 2.50);
        double dangerZonePrice = GetDisplayPrice("DangerZone", 2.50);
        double horizonPrice = GetDisplayPrice("Horizon", 2.00);
        double chroma3Price = GetDisplayPrice("Chroma3", 2.00);
        double spectrum2Price = GetDisplayPrice("Spectrum2", 2.50);
        double cs20Price = GetDisplayPrice("CS20", 1.50);
        
        CPH.SendMessage($"📦 Cases & Prices — Dreams(${dreamsPrice:F2}) | Recoil(${recoilPrice:F2}) | Kilowatt(${kilowattPrice:F2}) | Revolution(${revolutionPrice:F2}) | Fracture(${fracturePrice:F2}) | Clutch(${clutchPrice:F2}) | Prisma(${prismaPrice:F2}) | DangerZone(${dangerZonePrice:F2}) | Horizon(${horizonPrice:F2}) | Chroma3(${chroma3Price:F2}) | Spectrum2(${spectrum2Price:F2}) | CS20(${cs20Price:F2}) — Type !setcase [name] then !case to open!");
        return true;
    }
    
    private double GetDisplayPrice(string caseName, double defaultPrice)
    {
        double customPrice = CPH.GetGlobalVar<double>("Price_" + caseName);
        return customPrice > 0 ? customPrice : defaultPrice;
    }
}
ACTION: CaseStats_Command (!casestats)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        int dreams = CPH.GetGlobalVar<int>("Dreams_Opened");
        int recoil = CPH.GetGlobalVar<int>("Recoil_Opened");
        int kilowatt = CPH.GetGlobalVar<int>("Kilowatt_Opened");
        int revolution = CPH.GetGlobalVar<int>("Revolution_Opened");
        int fracture = CPH.GetGlobalVar<int>("Fracture_Opened");
        int clutch = CPH.GetGlobalVar<int>("Clutch_Opened");
        int prisma = CPH.GetGlobalVar<int>("Prisma_Opened");
        int dangerzone = CPH.GetGlobalVar<int>("DangerZone_Opened");
        int horizon = CPH.GetGlobalVar<int>("Horizon_Opened");
        int chroma3 = CPH.GetGlobalVar<int>("Chroma3_Opened");
        int spectrum2 = CPH.GetGlobalVar<int>("Spectrum2_Opened");
        int cs20 = CPH.GetGlobalVar<int>("CS20_Opened");

        CPH.SendMessage("📦 Case Stats — Dreams: " + dreams + " | Recoil: " + recoil + " | Kilowatt: " + kilowatt + " | Revolution: " + revolution + " | Fracture: " + fracture + " | Clutch: " + clutch + " | Prisma: " + prisma + " | DangerZone: " + dangerzone + " | Horizon: " + horizon + " | Chroma3: " + chroma3 + " | Spectrum2: " + spectrum2 + " | CS20: " + cs20);
        return true;
    }
}
CAPSULE & STICKER ACTIONS
ACTION: SetCapsule_Command (!setcapsule) - Shows Custom Price
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string capsuleName = rawInput.Replace("!setcapsule", "").Trim();
        
        if (string.IsNullOrEmpty(capsuleName))
        {
            CPH.SendMessage("📯 " + user + " Usage: !setcapsule [capsule name]\n\nAvailable capsules: Recoil, Dreams, Championship, Legends, CS20\n\nExample: !setcapsule Recoil");
            return false;
        }
        
        string[] validCapsules = { "Recoil", "Dreams", "Championship", "Legends", "CS20" };
        bool isValid = false;
        
        foreach (string valid in validCapsules)
        {
            if (capsuleName.Equals(valid, StringComparison.OrdinalIgnoreCase))
            {
                capsuleName = valid;
                isValid = true;
                break;
            }
        }
        
        if (!isValid)
        {
            CPH.SendMessage("❌ " + user + " '" + capsuleName + "' isn't a valid capsule.\n\nAvailable capsules: Recoil, Dreams, Championship, Legends, CS20\n\nExample: !setcapsule Recoil");
            return false;
        }
        
        CPH.SetGlobalVar(user + "_LastCapsule", capsuleName, true);
        
        // Get cost (check for custom price)
        double cost = GetBaseCapsuleCost(capsuleName);
        double customPrice = CPH.GetGlobalVar<double>("Price_" + capsuleName);
        if (customPrice > 0) cost = customPrice;
        
        CPH.SendMessage("📯 " + user + " switched to " + capsuleName + " capsule! ($" + cost.ToString("F2") + " per open) | Type !opencapsule to open it.");
        return true;
    }
    
    private double GetBaseCapsuleCost(string capsuleName)
    {
        if (capsuleName == "Recoil") return 0.50;
        if (capsuleName == "Dreams" || capsuleName == "CS20") return 1.00;
        if (capsuleName == "Championship") return 2.00;
        if (capsuleName == "Legends") return 3.00;
        return 0.50;
    }
}
ACTION: OpenCapsule_Command (!opencapsule)
Must have Sub-Action: Run Action → CS2_OpenCapsule

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string currentCapsule = CPH.GetGlobalVar<string>(user + "_LastCapsule");
        
        if (string.IsNullOrEmpty(currentCapsule))
        {
            CPH.SendMessage("📯 " + user + " You haven't selected a sticker capsule. Type !setcapsule Recoil then !opencapsule to open it.");
            return false;
        }
        
        // Get base cost
        double cost = GetBaseCapsuleCost(currentCapsule);
        // Check for custom price override
        double customPrice = CPH.GetGlobalVar<double>("Price_" + currentCapsule);
        if (customPrice > 0) cost = customPrice;
        
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        
        if (balance < cost)
        {
            double shortfall = cost - balance;
            CPH.SendMessage($"❌ {user} You need ${cost:F2} to open a {currentCapsule} capsule. You have ${balance:F2}. Type !daily or !sell to get more money.");
            return false;
        }
        
        balance -= cost;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        CPH.SetGlobalVar("User", user, true);
        CPH.SetGlobalVar("CurrentCapsule", currentCapsule, true);
        
        CPH.SendMessage($"🔑 {user} bought a {currentCapsule} capsule for ${cost:F2} | Balance: ${balance:F2}");
        
        return true;
    }
    
    private double GetBaseCapsuleCost(string capsuleName)
    {
        if (capsuleName == "Recoil") return 0.50;
        if (capsuleName == "Dreams" || capsuleName == "CS20") return 1.00;
        if (capsuleName == "Championship") return 2.00;
        if (capsuleName == "Legends") return 3.00;
        return 0.50;
    }
}
ACTION: GiftCapsule_Command (!giftcapsule) - With Quantity Support
Must have Sub-Action: Run Action → CS2_OpenCapsule

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string sender = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!giftcapsule"))
        {
            int cmdIndex = afterCommand.IndexOf("!giftcapsule");
            afterCommand = afterCommand.Substring(cmdIndex + 12).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 1)
        {
            CPH.SendMessage(sender + " Usage: !giftcapsule [quantity] @username [name] | Example: !giftcapsule 3 @user Recoil");
            return false;
        }
        
        int quantity = 1;
        string target = "";
        string capsuleName = "Recoil";
        string[] validCapsules = { "recoil", "dreams", "championship", "legends", "cs20" };
        
        int partIndex = 0;
        
        int testQuantity;
        if (int.TryParse(parts[0], out testQuantity))
        {
            quantity = Math.Min(testQuantity, 10);
            partIndex = 1;
        }
        
        if (parts.Length <= partIndex)
        {
            CPH.SendMessage(sender + " Please specify a username!");
            return false;
        }
        
        for (int i = partIndex; i < parts.Length; i++)
        {
            string cleanPart = parts[i].Replace("@", "").Trim().ToLower();
            bool isValidCapsule = false;
            foreach (string valid in validCapsules)
            {
                if (cleanPart == valid)
                {
                    isValidCapsule = true;
                    if (cleanPart == "recoil") capsuleName = "Recoil";
                    else if (cleanPart == "dreams") capsuleName = "Dreams";
                    else if (cleanPart == "championship") capsuleName = "Championship";
                    else if (cleanPart == "legends") capsuleName = "Legends";
                    else if (cleanPart == "cs20") capsuleName = "CS20";
                    break;
                }
            }
            if (!isValidCapsule && string.IsNullOrEmpty(target))
            {
                target = cleanPart;
            }
        }
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage(sender + " Please specify a username!");
            return false;
        }
        
        double targetBalance = CPH.GetGlobalVar<double>(target + "_Balance");
        if (targetBalance == 0 && CPH.GetGlobalVar<string>(target + "_Items") == null)
        {
            CPH.SendMessage(sender + " User @" + target + " hasn't played the bot yet!");
            return false;
        }
        
        double cost = GetCapsuleCost(capsuleName) * quantity;
        
        double senderBalance = CPH.GetGlobalVar<double>(sender + "_Balance");
        if (senderBalance < cost)
        {
            CPH.SendMessage(sender + " You need $" + cost.ToString("F2") + " to gift " + quantity + " " + capsuleName + " capsule(s)!");
            return false;
        }
        
        senderBalance -= cost;
        CPH.SetGlobalVar(sender + "_Balance", senderBalance, true);
        
        for (int i = 0; i < quantity; i++)
        {
            CPH.SetGlobalVar("User", target, true);
            CPH.SetGlobalVar("CurrentCapsule", capsuleName, true);
            CPH.SetGlobalVar(target + "_LastCapsule", capsuleName, true);
            System.Threading.Thread.Sleep(500);
            CPH.RunAction("CS2_OpenCapsule");
        }
        
        CPH.SendMessage("🎁 " + sender + " gifted " + quantity + " " + capsuleName + " capsule(s) to " + target + "!");
        return true;
    }
    
    private double GetCapsuleCost(string capsuleName)
    {
        if (capsuleName == "Recoil") return 0.50;
        if (capsuleName == "Dreams" || capsuleName == "CS20") return 1.00;
        if (capsuleName == "Championship") return 2.00;
        if (capsuleName == "Legends") return 3.00;
        return 0.50;
    }
}
ACTION: Capsules_Command (!capsules) - Shows Custom Prices
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        double recoilPrice = GetDisplayPrice("Recoil", 0.50);
        double dreamsPrice = GetDisplayPrice("Dreams", 1.00);
        double championshipPrice = GetDisplayPrice("Championship", 2.00);
        double legendsPrice = GetDisplayPrice("Legends", 3.00);
        double cs20Price = GetDisplayPrice("CS20", 1.00);
        
        CPH.SendMessage($"📯 Sticker Capsules — Recoil(${recoilPrice:F2}) | Dreams(${dreamsPrice:F2}) | Championship(${championshipPrice:F2}) | Legends(${legendsPrice:F2}) | CS20(${cs20Price:F2}) — Gold Tiers: 👑Common($30) 👑Rare($75) 👑Epic($150) 👑Legendary($300) — Type !setcapsule [name] then !opencapsule to open!");
        return true;
    }
    
    private double GetDisplayPrice(string capsuleName, double defaultPrice)
    {
        double customPrice = CPH.GetGlobalVar<double>("Price_" + capsuleName);
        return customPrice > 0 ? customPrice : defaultPrice;
    }
}
ACTION: Stickers_Command (!stickers) - Two-Message Format
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string items = CPH.GetGlobalVar<string>(user + "_Items");
        
        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage("📯 " + user + " You have no stickers! Type !setcapsule then !opencapsule to get some!");
            return true;
        }
        
        string[] itemList = items.Split(';');
        int starCount = 0, sparkleCount = 0, cometCount = 0, fireCount = 0, crownCount = 0;
        double totalValue = 0;
        string stickerList = "";
        int stickerCount = 0;
        
        foreach (string item in itemList)
        {
            if (item.Contains("⭐")) { starCount++; totalValue += 0.10; }
            else if (item.Contains("✨")) { sparkleCount++; totalValue += 0.50; }
            else if (item.Contains("💫")) { cometCount++; totalValue += 2.00; }
            else if (item.Contains("🔥")) { fireCount++; totalValue += 10.00; }
            else if (item.Contains("👑")) 
            { 
                crownCount++; 
                if (item.Contains("Legendary")) totalValue += 300.00;
                else if (item.Contains("Epic")) totalValue += 150.00;
                else if (item.Contains("Rare")) totalValue += 75.00;
                else totalValue += 30.00;
            }
        }
        
        // Build sticker list (first 8 stickers)
        stickerCount = 0;
        for (int i = 0; i < itemList.Length && stickerCount < 8; i++)
        {
            string item = itemList[i];
            if (item.Contains("⭐") || item.Contains("✨") || item.Contains("💫") || item.Contains("🔥") || item.Contains("👑"))
            {
                string shortItem = item.Length > 30 ? item.Substring(0, 27) + "..." : item;
                stickerList += (stickerCount + 1) + "." + shortItem + " ";
                stickerCount++;
            }
        }
        
        // First message: counts and value
        CPH.SendMessage($"📯 {user}'s Stickers | ⭐:{starCount} ✨:{sparkleCount} 💫:{cometCount} 🔥:{fireCount} 👑:{crownCount} | 💰 ${totalValue:F2}");
        
        // Second message: examples
        if (stickerCount > 0)
        {
            CPH.SendMessage($"📯 {user}'s Stickers | {stickerList}| Type !apply # # to apply, !sell # to sell");
        }
        else
        {
            CPH.SendMessage($"📯 {user} has no stickers to display.");
        }
        
        return true;
    }
}
ACTION: ApplySticker_Command (!apply) - With Tracking
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        // Clean the input
        string cleanInput = rawInput;
        if (cleanInput.StartsWith("!apply"))
        {
            cleanInput = cleanInput.Substring(6).Trim();
        }
        
        if (cleanInput.StartsWith("@"))
        {
            int spaceIndex = cleanInput.IndexOf(' ');
            if (spaceIndex > 0)
            {
                cleanInput = cleanInput.Substring(spaceIndex + 1);
            }
        }
        
        string[] parts = cleanInput.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage($"📯 {user} Usage: !apply [sticker number] [weapon number]\n\nUse !stickers to see sticker numbers, !inv to see weapon numbers\n\nExample: !apply 1 3");
            return false;
        }
        
        int stickerNum, weaponNum;
        if (!int.TryParse(parts[0], out stickerNum) || !int.TryParse(parts[1], out weaponNum))
        {
            CPH.SendMessage($"❌ {user} Please use valid numbers! Example: !apply 1 3");
            return false;
        }
        
        string items = CPH.GetGlobalVar<string>(user + "_Items");
        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage($"❌ {user} You have no stickers or weapons!");
            return false;
        }
        
        string[] itemList = items.Split(';');
        
        // Find the sticker
        int stickerIndex = -1;
        int currentStickerNum = 0;
        for (int i = 0; i < itemList.Length; i++)
        {
            if (itemList[i].Contains("⭐") || itemList[i].Contains("✨") || itemList[i].Contains("💫") || itemList[i].Contains("🔥") || itemList[i].Contains("👑"))
            {
                currentStickerNum++;
                if (currentStickerNum == stickerNum)
                {
                    stickerIndex = i;
                    break;
                }
            }
        }
        
        if (stickerIndex == -1)
        {
            CPH.SendMessage($"❌ {user} Invalid sticker number! Type !stickers to see your stickers numbered.");
            return false;
        }
        
        // Find the weapon
        int weaponIndex = -1;
        int currentWeaponNum = 0;
        for (int i = 0; i < itemList.Length; i++)
        {
            if (itemList[i].Contains("🔷") || itemList[i].Contains("🟣") || itemList[i].Contains("💗") || itemList[i].Contains("🔴") || itemList[i].Contains("🟡"))
            {
                currentWeaponNum++;
                if (currentWeaponNum == weaponNum)
                {
                    weaponIndex = i;
                    break;
                }
            }
        }
        
        if (weaponIndex == -1)
        {
            CPH.SendMessage($"❌ {user} Invalid weapon number! Type !inv to see your weapons numbered.");
            return false;
        }
        
        string sticker = itemList[stickerIndex];
        string weapon = itemList[weaponIndex];
        
        // Get sticker name (without emoji)
        string stickerName = GetStickerName(sticker);
        
        // Calculate sticker value
        double stickerValue = GetStickerValue(sticker);
        double boost = stickerValue * 0.25;
        
        // Remove sticker from inventory
        var newList = new List<string>(itemList);
        newList.RemoveAt(stickerIndex);
        CPH.SetGlobalVar(user + "_Items", string.Join(";", newList), true);
        
        // Add boost to balance
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        balance += boost;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        // Track applied stickers
        string appliedKey = user + "_AppliedStickers";
        string appliedStickers = CPH.GetGlobalVar<string>(appliedKey);
        
        // Clean up old entries (remove old format)
        if (!string.IsNullOrEmpty(appliedStickers))
        {
            string[] oldEntries = appliedStickers.Split(new[] { " | " }, StringSplitOptions.None);
            List<string> cleanEntries = new List<string>();
            foreach (string entry in oldEntries)
            {
                if (!entry.Contains("Weapon ") && !entry.Contains(":Gold"))
                {
                    cleanEntries.Add(entry);
                }
            }
            appliedStickers = string.Join(" | ", cleanEntries);
        }
        
        // Get short weapon name
        string weaponName = weapon.Length > 35 ? weapon.Substring(0, 32) + "..." : weapon;
        string newApplied = $"{weaponName} ← {stickerName}";
        
        if (string.IsNullOrEmpty(appliedStickers))
        {
            appliedStickers = newApplied;
        }
        else
        {
            appliedStickers = newApplied + " | " + appliedStickers;
        }
        
        // Keep only last 10 entries
        string[] entries = appliedStickers.Split(new[] { " | " }, StringSplitOptions.None);
        if (entries.Length > 10)
        {
            appliedStickers = string.Join(" | ", entries, 0, 10);
        }
        
        CPH.SetGlobalVar(appliedKey, appliedStickers, true);
        
        // Track total stickers applied
        int totalApplied = CPH.GetGlobalVar<int>(user + "_TotalApplied");
        totalApplied++;
        CPH.SetGlobalVar(user + "_TotalApplied", totalApplied, true);
        
        CPH.SendMessage($"✨ {user} applied {stickerName} to {weaponName}! +${boost:F2} to balance");
        return true;
    }
    
    private double GetStickerValue(string sticker)
    {
        if (sticker.Contains("👑"))
        {
            if (sticker.Contains("Legendary")) return 300.00;
            if (sticker.Contains("Epic")) return 150.00;
            if (sticker.Contains("Rare")) return 75.00;
            return 30.00;
        }
        if (sticker.Contains("🔥")) return 10.00;
        if (sticker.Contains("💫")) return 2.00;
        if (sticker.Contains("✨")) return 0.50;
        if (sticker.Contains("⭐")) return 0.10;
        return 0;
    }
    
    private string GetStickerName(string sticker)
    {
        string name = sticker;
        
        // Remove the emoji prefix
        if (name.Length > 2)
        {
            name = name.Substring(2).Trim();
        }
        
        // Remove tier tag
        if (name.Contains("[Common]")) name = name.Replace("[Common]", "").Trim();
        if (name.Contains("[Rare]")) name = name.Replace("[Rare]", "").Trim();
        if (name.Contains("[Epic]")) name = name.Replace("[Epic]", "").Trim();
        if (name.Contains("[Legendary]")) name = name.Replace("[Legendary]", "").Trim();
        
        return name;
    }
}
ACTION: Applied_Command (!applied)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string appliedKey = user + "_AppliedStickers";
        string appliedStickers = CPH.GetGlobalVar<string>(appliedKey);
        int totalApplied = CPH.GetGlobalVar<int>(user + "_TotalApplied");
        
        if (string.IsNullOrEmpty(appliedStickers))
        {
            CPH.SendMessage($"📯 {user} hasn't applied any stickers yet. Type !apply [sticker#] [weapon#] to apply a sticker.");
            return true;
        }
        
        // Limit display length
        string display = appliedStickers;
        if (display.Length > 450)
        {
            display = display.Substring(0, 447) + "...";
        }
        
        CPH.SendMessage($"📯 {user} has applied {totalApplied} sticker" + (totalApplied != 1 ? "s" : "") + " | {display}");
        return true;
    }
}
ACTION: StickerValue_Command (!stickervalue)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        CPH.SendMessage("📯 Sticker Rarity: ⭐ Common($0.10) | ✨ Uncommon($0.50) | 💫 Rare($2.00) | 🔥 Epic($10.00) | 👑 Legendary($30-$300) | Gold Tiers: 👑Common($30) 👑Rare($75) 👑Epic($150) 👑Legendary($300) | Capsules: Recoil($0.50), Dreams($1.00), Championship($2.00), Legends($3.00), CS20($1.00)");
        return true;
    }
}
ECONOMY ACTIONS
ACTION: Inv_Command (!inv) - Two-Message Format with Sticker Counts
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string items = CPH.GetGlobalVar<string>(user + "_Items");
        string appliedKey = user + "_AppliedStickers";
        string appliedStickers = CPH.GetGlobalVar<string>(appliedKey);

        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage("📦 " + user + " Your inventory is empty. Type !setcase Dreams then !case to open a case!");
            return true;
        }

        string[] itemList = items.Split(';');
        
        int countGold = 0, countRed = 0, countPink = 0, countPurple = 0, countBlue = 0;
        double totalValue = 0;
        string weaponList = "";
        int weaponCount = 0;
        
        // Parse applied stickers into a lookup
        var stickerLookup = new Dictionary<int, int>();
        if (!string.IsNullOrEmpty(appliedStickers))
        {
            string[] stickers = appliedStickers.Split(new[] { " | " }, StringSplitOptions.None);
            foreach (string s in stickers)
            {
                if (s.Contains("←"))
                {
                    // Format: "weaponName ← stickerName" - we can't get index from this
                    // Sticker count on weapons is now tracked differently
                }
            }
        }
        
        foreach (string item in itemList)
        {
            if (item.Contains("🟡")) 
            { 
                countGold++; 
                if (item.Contains("Mythic")) totalValue += 2500.00;
                else if (item.Contains("Legendary")) totalValue += 1000.00;
                else if (item.Contains("Epic")) totalValue += 600.00;
                else if (item.Contains("Rare")) totalValue += 300.00;
                else totalValue += 150.00;
            }
            else if (item.Contains("🔴")) { countRed++; totalValue += 20.00; }
            else if (item.Contains("💗")) { countPink++; totalValue += 4.00; }
            else if (item.Contains("🟣")) { countPurple++; totalValue += 1.00; }
            else if (item.Contains("🔷")) { countBlue++; totalValue += 0.25; }
            
            if (item.Contains("StatTrak") && !item.Contains("🟡")) totalValue += totalValue;
            if (item.Contains("Factory New") && !item.Contains("🟡")) totalValue += totalValue;
            else if (item.Contains("Minimal Wear") && !item.Contains("🟡")) totalValue += totalValue / 2;
            else if (item.Contains("Well-Worn") && !item.Contains("🟡")) totalValue -= totalValue / 4;
            else if (item.Contains("Battle-Scarred") && !item.Contains("🟡")) totalValue -= totalValue / 2;
        }
        
        // Build weapon list (first 8 weapons)
        weaponCount = 0;
        for (int i = 0; i < itemList.Length && weaponCount < 8; i++)
        {
            string item = itemList[i];
            if (item.Contains("🔷") || item.Contains("🟣") || item.Contains("💗") || item.Contains("🔴") || item.Contains("🟡"))
            {
                string shortItem = item.Length > 30 ? item.Substring(0, 27) + "..." : item;
                weaponList += (weaponCount + 1) + "." + shortItem + " ";
                weaponCount++;
            }
        }
        
        // First message: counts and value
        CPH.SendMessage($"📦 {user}'s Inventory | 🟡:{countGold} 🔴:{countRed} 💗:{countPink} 🟣:{countPurple} 🔷:{countBlue} | 💰 ${totalValue:F2}");
        
        // Second message: weapons
        if (weaponCount > 0)
        {
            CPH.SendMessage($"📦 {user}'s Weapons | {weaponList}| Type !sell # to sell, !stickers for stickers");
        }
        else
        {
            CPH.SendMessage($"📦 {user} has no weapons visible. Type !sell to see your full inventory.");
        }
        
        return true;
    }
}
ACTION: Sell_Command (!sell)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string cleanInput = rawInput;
        if (cleanInput.StartsWith("@"))
        {
            int spaceIndex = cleanInput.IndexOf(' ');
            if (spaceIndex > 0) cleanInput = cleanInput.Substring(spaceIndex + 1);
        }
        
        if (cleanInput.StartsWith("!sell")) cleanInput = cleanInput.Substring(5).Trim();
        
        string items = CPH.GetGlobalVar<string>(user + "_Items");
        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage(user + " Your inventory is empty!");
            return false;
        }

        string[] itemList = items.Split(';');
        int sellIndex = -1;
        
        if (!string.IsNullOrEmpty(cleanInput))
        {
            if (!int.TryParse(cleanInput, out sellIndex))
            {
                CPH.SendMessage(user + " Please use a number! Example: !sell 3 | Type !sell alone to see your numbered inventory.");
                return false;
            }
        }
        
        if (sellIndex == -1)
        {
            string inventoryList = "";
            int shown = 0;
            for (int i = 0; i < itemList.Length && shown < 10; i++)
            {
                if (itemList[i].Contains("🔷") || itemList[i].Contains("🟣") || itemList[i].Contains("💗") || itemList[i].Contains("🔴") || itemList[i].Contains("🟡") ||
                    itemList[i].Contains("⭐") || itemList[i].Contains("✨") || itemList[i].Contains("💫") || itemList[i].Contains("🔥") || itemList[i].Contains("👑"))
                {
                    string shortItem = itemList[i].Length > 40 ? itemList[i].Substring(0, 37) + "..." : itemList[i];
                    inventoryList += (shown + 1) + ". " + shortItem + " | ";
                    shown++;
                }
            }
            if (itemList.Length > 10) inventoryList += "... and " + (itemList.Length - 10) + " more";
            
            CPH.SendMessage("📦 " + user + " Type !sell [number] to sell an item. Example: !sell 2\n\nYour items: " + inventoryList + "\n\nType !inv to see your full inventory, !sellhelp for more info.");
            return false;
        }
        
        sellIndex--;
        
        List<string> visibleItems = new List<string>();
        foreach (string item in itemList)
        {
            if (item.Contains("🔷") || item.Contains("🟣") || item.Contains("💗") || item.Contains("🔴") || item.Contains("🟡") ||
                item.Contains("⭐") || item.Contains("✨") || item.Contains("💫") || item.Contains("🔥") || item.Contains("👑"))
            {
                visibleItems.Add(item);
            }
        }
        
        if (sellIndex < 0 || sellIndex >= visibleItems.Count)
        {
            CPH.SendMessage(user + " Invalid item number! You have " + visibleItems.Count + " sellable items.");
            return false;
        }
        
        string soldItem = visibleItems[sellIndex];
        
        double value = CalculateItemValue(soldItem);
        
        var newList = new List<string>(itemList);
        newList.Remove(soldItem);
        CPH.SetGlobalVar(user + "_Items", string.Join(";", newList), true);

        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        balance += value;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        CPH.SetGlobalVar(user + "_OpenStreak", 0, true);
        
        CPH.SendMessage("💰 " + user + " sold " + soldItem + " for $" + value.ToString("F2") + " | New balance: $" + balance.ToString("F2") + " | Streak reset!");
        return true;
    }
    
    private double CalculateItemValue(string item)
    {
        double value = 0;
        
        if (item.Contains("🟡"))
        {
            if (item.Contains("Mythic")) value = 2500.00;
            else if (item.Contains("Legendary")) value = 1000.00;
            else if (item.Contains("Epic")) value = 600.00;
            else if (item.Contains("Rare")) value = 300.00;
            else value = 150.00;
        }
        else if (item.Contains("🔴")) value = 20.00;
        else if (item.Contains("💗")) value = 4.00;
        else if (item.Contains("🟣")) value = 1.00;
        else if (item.Contains("🔷")) value = 0.25;
        else if (item.Contains("👑"))
        {
            if (item.Contains("Legendary")) value = 300.00;
            else if (item.Contains("Epic")) value = 150.00;
            else if (item.Contains("Rare")) value = 75.00;
            else value = 30.00;
        }
        else if (item.Contains("🔥")) value = 10.00;
        else if (item.Contains("💫")) value = 2.00;
        else if (item.Contains("✨")) value = 0.50;
        else if (item.Contains("⭐")) value = 0.10;
        
        if (!item.Contains("⭐") && !item.Contains("✨") && !item.Contains("💫") && !item.Contains("🔥") && !item.Contains("👑"))
        {
            if (item.Contains("Factory New")) value *= 2.0;
            else if (item.Contains("Minimal Wear")) value *= 1.5;
            else if (item.Contains("Well-Worn")) value *= 0.75;
            else if (item.Contains("Battle-Scarred")) value *= 0.5;
        }
        
        if (item.Contains("StatTrak") && !item.Contains("🟡") && !item.Contains("👑")) value *= 2;
        
        return value;
    }
}
ACTION: SellAll_Command (!sellall)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string items = CPH.GetGlobalVar<string>(user + "_Items");

        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage(user + " Your inventory is empty! Nothing to sell.");
            return false;
        }

        string[] itemList = items.Split(';');
        double totalValue = 0;
        
        if (itemList.Length > 20)
        {
            CPH.SendMessage("⚠️ " + user + " You are about to sell ALL " + itemList.Length + " items! Type !sellallconfirm to confirm.");
            CPH.SetGlobalVar(user + "_PendingSellAll", "true", true);
            return true;
        }
        
        foreach (string item in itemList)
        {
            totalValue += CalculateItemValue(item);
        }
        
        CPH.SetGlobalVar(user + "_Items", "", true);
        
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        balance += totalValue;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        CPH.SetGlobalVar(user + "_OpenStreak", 0, true);
        CPH.SetGlobalVar(user + "_PendingSellAll", "", true);
        
        CPH.SendMessage("💰 " + user + " sold ALL " + itemList.Length + " items for $" + totalValue.ToString("F2") + " | New balance: $" + balance.ToString("F2") + " | Streak reset!");
        return true;
    }
    
    private double CalculateItemValue(string item)
    {
        double value = 0;
        
        if (item.Contains("🟡"))
        {
            if (item.Contains("Mythic")) value = 2500.00;
            else if (item.Contains("Legendary")) value = 1000.00;
            else if (item.Contains("Epic")) value = 600.00;
            else if (item.Contains("Rare")) value = 300.00;
            else value = 150.00;
        }
        else if (item.Contains("🔴")) value = 20.00;
        else if (item.Contains("💗")) value = 4.00;
        else if (item.Contains("🟣")) value = 1.00;
        else if (item.Contains("🔷")) value = 0.25;
        else if (item.Contains("👑"))
        {
            if (item.Contains("Legendary")) value = 300.00;
            else if (item.Contains("Epic")) value = 150.00;
            else if (item.Contains("Rare")) value = 75.00;
            else value = 30.00;
        }
        else if (item.Contains("🔥")) value = 10.00;
        else if (item.Contains("💫")) value = 2.00;
        else if (item.Contains("✨")) value = 0.50;
        else if (item.Contains("⭐")) value = 0.10;
        
        if (!item.Contains("⭐") && !item.Contains("✨") && !item.Contains("💫") && !item.Contains("🔥") && !item.Contains("👑"))
        {
            if (item.Contains("Factory New")) value *= 2.0;
            else if (item.Contains("Minimal Wear")) value *= 1.5;
            else if (item.Contains("Well-Worn")) value *= 0.75;
            else if (item.Contains("Battle-Scarred")) value *= 0.5;
        }
        
        if (item.Contains("StatTrak") && !item.Contains("🟡") && !item.Contains("👑")) value *= 2;
        
        return value;
    }
}
ACTION: SellAllConfirm_Command (!sellallconfirm)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string pending = CPH.GetGlobalVar<string>(user + "_PendingSellAll");
        
        if (pending != "true")
        {
            CPH.SendMessage(user + " You don't have a pending sell-all request. Type !sellall to sell your entire inventory.");
            return false;
        }
        
        string items = CPH.GetGlobalVar<string>(user + "_Items");
        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage(user + " Your inventory is empty!");
            CPH.SetGlobalVar(user + "_PendingSellAll", "", true);
            return false;
        }
        
        string[] itemList = items.Split(';');
        double totalValue = 0;
        
        foreach (string item in itemList)
        {
            if (item.Contains("🟡"))
            {
                if (item.Contains("Mythic")) totalValue += 2500.00;
                else if (item.Contains("Legendary")) totalValue += 1000.00;
                else if (item.Contains("Epic")) totalValue += 600.00;
                else if (item.Contains("Rare")) totalValue += 300.00;
                else totalValue += 150.00;
            }
            else if (item.Contains("🔴")) totalValue += 20.00;
            else if (item.Contains("💗")) totalValue += 4.00;
            else if (item.Contains("🟣")) totalValue += 1.00;
            else if (item.Contains("🔷")) totalValue += 0.25;
            else if (item.Contains("👑"))
            {
                if (item.Contains("Legendary")) totalValue += 300.00;
                else if (item.Contains("Epic")) totalValue += 150.00;
                else if (item.Contains("Rare")) totalValue += 75.00;
                else totalValue += 30.00;
            }
            else if (item.Contains("🔥")) totalValue += 10.00;
            else if (item.Contains("💫")) totalValue += 2.00;
            else if (item.Contains("✨")) totalValue += 0.50;
            else if (item.Contains("⭐")) totalValue += 0.10;
            
            if (item.Contains("StatTrak") && !item.Contains("🟡") && !item.Contains("👑")) totalValue += totalValue;
            if (item.Contains("Factory New") && !item.Contains("🟡") && !item.Contains("👑")) totalValue += totalValue;
            else if (item.Contains("Minimal Wear") && !item.Contains("🟡") && !item.Contains("👑")) totalValue += totalValue / 2;
            else if (item.Contains("Well-Worn") && !item.Contains("🟡") && !item.Contains("👑")) totalValue -= totalValue / 4;
            else if (item.Contains("Battle-Scarred") && !item.Contains("🟡") && !item.Contains("👑")) totalValue -= totalValue / 2;
        }
        
        CPH.SetGlobalVar(user + "_Items", "", true);
        
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        balance += totalValue;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        CPH.SetGlobalVar(user + "_OpenStreak", 0, true);
        CPH.SetGlobalVar(user + "_PendingSellAll", "", true);
        
        CPH.SendMessage("💰 " + user + " sold ALL " + itemList.Length + " items for $" + totalValue.ToString("F2") + " | New balance: $" + balance.ToString("F2") + " | Streak reset!");
        return true;
    }
}
ACTION: SellHelp_Command (!sellhelp)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        
        CPH.SendMessage("💰 " + user + " SELLING GUIDE:\n\n!sell - Show your numbered inventory\n!sell [number] - Sell that specific item\n!sellall - Sell your entire inventory\n\nExample: !sell 3 sells your third item\n\nSelling resets your open streak!\n\nType !inv to see your inventory value");
        return true;
    }
}
ACTION: Balance_Command (!balance)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        CPH.SendMessage("💰 " + user + "'s Balance: $" + balance.ToString("F2") + " | Tip: Type !daily for a chance at $50,000!");
        return true;
    }
}
ACTION: Daily_Command (!daily)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string lastDailyStr = CPH.GetGlobalVar<string>(user + "_LastDaily");

        if (!string.IsNullOrEmpty(lastDailyStr))
        {
            DateTime lastDaily = DateTime.Parse(lastDailyStr);
            TimeSpan elapsed = DateTime.UtcNow - lastDaily;
            if (elapsed.TotalHours < 24)
            {
                int hours = 24 - (int)elapsed.TotalHours;
                CPH.SendMessage(user + " daily already claimed! " + hours + "h remaining.");
                return false;
            }
        }

        var rng = new Random();
        int roll = rng.Next(1, 1000001);
        double reward;
        
        if (roll == 1000000)
        {
            reward = 50000.00;
            CPH.SendMessage("🎉🎉🎉 " + user + " HIT THE DAILY JACKPOT! $50,000! 🎉🎉🎉");
        }
        else if (roll >= 990001)
        {
            reward = rng.Next(2001, 5001);
        }
        else if (roll >= 970001)
        {
            reward = rng.Next(501, 2000);
        }
        else if (roll >= 850001)
        {
            reward = rng.Next(151, 500);
        }
        else
        {
            reward = rng.Next(10, 51);
        }
        
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        balance += reward;
        
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        CPH.SetGlobalVar(user + "_LastDaily", DateTime.UtcNow.ToString(), true);
        
        CPH.SendMessage("🎁 " + user + " rolled " + roll + " and won $" + reward.ToString("F2") + " from !daily! | Balance: $" + balance.ToString("F2") + " | 1 in 1,000,000 chance for $50,000 jackpot!");
        return true;
    }
}
ACTION: Tradeup_Command (!tradeup) - Full Expanded
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        bool isSticker = rawInput.Contains("sticker");
        string rarity = "";
        
        if (isSticker)
        {
            if (rawInput.Contains("blue")) rarity = "blue";
            else if (rawInput.Contains("purple")) rarity = "purple";
            else if (rawInput.Contains("pink")) rarity = "pink";
            else if (rawInput.Contains("red")) rarity = "red";
        }
        else
        {
            if (rawInput.Contains("blue")) rarity = "blue";
            else if (rawInput.Contains("purple")) rarity = "purple";
            else if (rawInput.Contains("pink")) rarity = "pink";
            else if (rawInput.Contains("red")) rarity = "red";
        }
        
        if (string.IsNullOrEmpty(rarity))
        {
            CPH.SendMessage(user + " Usage: !tradeup [blue/purple/pink/red]\n!tradeup sticker [blue/purple/pink/red]\n\nTrade up to the next rarity! Red (5 items) → Gold, others (10 items) → next tier\n\nExample: !tradeup blue");
            return false;
        }
        
        string items = CPH.GetGlobalVar<string>(user + "_Items");
        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage(user + " Your inventory is empty! Open some cases first with !setcase then !case");
            return false;
        }
        
        string[] itemList = items.Split(';');
        
        if (isSticker)
        {
            return ExecuteStickerTradeUp(user, itemList, rarity);
        }
        else
        {
            return ExecuteWeaponTradeUp(user, itemList, rarity);
        }
    }
    
    private bool ExecuteWeaponTradeUp(string user, string[] itemList, string rarity)
    {
        var rng = new Random();
        string requiredEmoji = "";
        int requiredCount = 10;
        string targetRarity = "";
        
        switch (rarity)
        {
            case "blue":
                requiredEmoji = "🔷";
                requiredCount = 10;
                targetRarity = "Purple";
                break;
            case "purple":
                requiredEmoji = "🟣";
                requiredCount = 10;
                targetRarity = "Pink";
                break;
            case "pink":
                requiredEmoji = "💗";
                requiredCount = 10;
                targetRarity = "Red";
                break;
            case "red":
                requiredEmoji = "🔴";
                requiredCount = 5;
                targetRarity = "Gold";
                break;
            default:
                CPH.SendMessage(user + " Invalid rarity! Use: blue, purple, pink, or red");
                return false;
        }
        
        List<string> itemsOfRarity = new List<string>();
        foreach (string item in itemList)
        {
            if (item.Contains(requiredEmoji) && !item.Contains("⭐") && !item.Contains("✨") && !item.Contains("💫") && !item.Contains("🔥") && !item.Contains("👑"))
            {
                itemsOfRarity.Add(item);
            }
        }
        
        if (itemsOfRarity.Count < requiredCount)
        {
            CPH.SendMessage(user + " You need " + requiredCount + " " + GetRarityName(requiredEmoji) + " weapons to trade up! You have " + itemsOfRarity.Count + ".\n\nOpen more cases or trade with other viewers!");
            return false;
        }
        
        var newList = new List<string>(itemList);
        int removed = 0;
        for (int i = newList.Count - 1; i >= 0 && removed < requiredCount; i--)
        {
            if (newList[i].Contains(requiredEmoji) && !newList[i].Contains("⭐") && !newList[i].Contains("✨") && !newList[i].Contains("💫") && !newList[i].Contains("🔥") && !newList[i].Contains("👑"))
            {
                newList.RemoveAt(i);
                removed++;
            }
        }
        
        string currentCase = CPH.GetGlobalVar<string>("CurrentCase");
        string casePrefix = string.IsNullOrEmpty(currentCase) ? "Dreams" : currentCase.Replace(" ", "").Replace("&", "").Replace("-", "");
        string targetListKey = casePrefix + targetRarity + "List";
        string targetList = CPH.GetGlobalVar<string>(targetListKey);
        
        if (string.IsNullOrEmpty(targetList))
        {
            CPH.SendMessage(user + " Trade-up failed: No " + targetRarity + " skins found for " + currentCase + " case.\n\nThis is a setup issue. Make sure your persisted globals include " + casePrefix + targetRarity + "List");
            return false;
        }
        
        string[] targets = targetList.Split(';');
        string newSkin = targets[rng.Next(0, targets.Length)].Trim();
        
        string[] conditions = { "Factory New", "Minimal Wear", "Field-Tested", "Well-Worn", "Battle-Scarred" };
        int conditionRoll = rng.Next(1, 101);
        int conditionIndex = 0;
        if (conditionRoll <= 5) conditionIndex = 0;
        else if (conditionRoll <= 20) conditionIndex = 1;
        else if (conditionRoll <= 70) conditionIndex = 2;
        else if (conditionRoll <= 90) conditionIndex = 3;
        else conditionIndex = 4;
        
        string condition = conditions[conditionIndex];
        
        bool isStatTrak = rng.Next(1, 101) <= 10;
        string statTrakTag = isStatTrak ? " (StatTrak™)" : "";
        
        string emoji = GetRarityEmoji(targetRarity);
        string newItem = emoji + " " + newSkin + " (" + condition + ")" + statTrakTag;
        
        if (targetRarity == "Gold")
        {
            int goldTierRoll = rng.Next(1, 1001);
            if (goldTierRoll <= 500) newItem += " [Common]";
            else if (goldTierRoll <= 800) newItem += " [Rare]";
            else if (goldTierRoll <= 950) newItem += " [Epic]";
            else if (goldTierRoll <= 995) newItem += " [Legendary]";
            else newItem += " [Mythic]";
        }
        
        newList.Add(newItem);
        
        CPH.SetGlobalVar(user + "_Items", string.Join(";", newList), true);
        
        int tradeupCount = CPH.GetGlobalVar<int>(user + "_TradeupCount");
        tradeupCount++;
        CPH.SetGlobalVar(user + "_TradeupCount", tradeupCount, true);
        
        CPH.SendMessage("🎉 " + user + " traded up " + requiredCount + " " + GetRarityName(requiredEmoji) + " and got: " + newItem + "!");
        return true;
    }
    
    private bool ExecuteStickerTradeUp(string user, string[] itemList, string rarity)
    {
        var rng = new Random();
        string requiredEmoji = "";
        int requiredCount = 10;
        string targetEmoji = "";
        string targetName = "";
        
        switch (rarity)
        {
            case "blue":
                requiredEmoji = "⭐";
                requiredCount = 10;
                targetEmoji = "✨";
                targetName = "Purple";
                break;
            case "purple":
                requiredEmoji = "✨";
                requiredCount = 10;
                targetEmoji = "💫";
                targetName = "Pink";
                break;
            case "pink":
                requiredEmoji = "💫";
                requiredCount = 10;
                targetEmoji = "🔥";
                targetName = "Red";
                break;
            case "red":
                requiredEmoji = "🔥";
                requiredCount = 5;
                targetEmoji = "👑";
                targetName = "Gold";
                break;
            default:
                CPH.SendMessage(user + " Invalid sticker rarity! Use: blue, purple, pink, or red");
                return false;
        }
        
        List<string> itemsOfRarity = new List<string>();
        foreach (string item in itemList)
        {
            if (item.Contains(requiredEmoji))
            {
                itemsOfRarity.Add(item);
            }
        }
        
        if (itemsOfRarity.Count < requiredCount)
        {
            CPH.SendMessage(user + " You need " + requiredCount + " " + GetStickerRarityName(requiredEmoji) + " stickers to trade up! You have " + itemsOfRarity.Count + ".");
            return false;
        }
        
        var newList = new List<string>(itemList);
        int removed = 0;
        for (int i = newList.Count - 1; i >= 0 && removed < requiredCount; i--)
        {
            if (newList[i].Contains(requiredEmoji))
            {
                newList.RemoveAt(i);
                removed++;
            }
        }
        
        string currentCapsule = CPH.GetGlobalVar<string>(user + "_LastCapsule");
        if (string.IsNullOrEmpty(currentCapsule)) currentCapsule = "Recoil";
        
        string targetListKey = currentCapsule + "Sticker" + targetName + "List";
        string targetList = CPH.GetGlobalVar<string>(targetListKey);
        
        if (string.IsNullOrEmpty(targetList))
        {
            CPH.SendMessage(user + " Trade-up failed: No " + targetName + " stickers found for " + currentCapsule + " capsule!");
            return false;
        }
        
        string[] targets = targetList.Split(';');
        string newStickerName = targets[rng.Next(0, targets.Length)].Trim();
        
        string newSticker = targetEmoji + " " + newStickerName;
        
        if (targetEmoji == "👑")
        {
            int goldTierRoll = rng.Next(1, 1001);
            if (goldTierRoll <= 500) newSticker += " [Common]";
            else if (goldTierRoll <= 800) newSticker += " [Rare]";
            else if (goldTierRoll <= 950) newSticker += " [Epic]";
            else newSticker += " [Legendary]";
        }
        
        newList.Add(newSticker);
        
        CPH.SetGlobalVar(user + "_Items", string.Join(";", newList), true);
        
        CPH.SendMessage("🎉 " + user + " traded up " + requiredCount + " " + GetStickerRarityName(requiredEmoji) + " stickers and got: " + newSticker + "!");
        return true;
    }
    
    private string GetRarityName(string emoji)
    {
        if (emoji == "🔷") return "Blue";
        if (emoji == "🟣") return "Purple";
        if (emoji == "💗") return "Pink";
        if (emoji == "🔴") return "Red";
        if (emoji == "🟡") return "Gold";
        return "Unknown";
    }
    
    private string GetRarityEmoji(string rarity)
    {
        if (rarity == "Purple") return "🟣";
        if (rarity == "Pink") return "💗";
        if (rarity == "Red") return "🔴";
        if (rarity == "Gold") return "🟡";
        return "🔷";
    }
    
    private string GetStickerRarityName(string emoji)
    {
        if (emoji == "⭐") return "High Grade";
        if (emoji == "✨") return "Remarkable";
        if (emoji == "💫") return "Exotic";
        if (emoji == "🔥") return "Extraordinary";
        if (emoji == "👑") return "Contraband";
        return "Unknown";
    }
}
ACTION: InventoryValue_Command (!inventoryvalue)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string items = CPH.GetGlobalVar<string>(user + "_Items");

        if (string.IsNullOrEmpty(items)) { CPH.SendMessage(user + " your inventory is empty!"); return true; }

        double total = 0;
        foreach (string item in items.Split(';'))
        {
            if (item.Contains("🟡"))
            {
                if (item.Contains("Mythic")) total += 2500.00;
                else if (item.Contains("Legendary")) total += 1000.00;
                else if (item.Contains("Epic")) total += 600.00;
                else if (item.Contains("Rare")) total += 300.00;
                else total += 150.00;
            }
            else if (item.Contains("🔴")) total += 20.00;
            else if (item.Contains("💗")) total += 4.00;
            else if (item.Contains("🟣")) total += 1.00;
            else if (item.Contains("🔷")) total += 0.25;
            else if (item.Contains("👑"))
            {
                if (item.Contains("Legendary")) total += 300.00;
                else if (item.Contains("Epic")) total += 150.00;
                else if (item.Contains("Rare")) total += 75.00;
                else total += 30.00;
            }
            else if (item.Contains("🔥")) total += 10.00;
            else if (item.Contains("💫")) total += 2.00;
            else if (item.Contains("✨")) total += 0.50;
            else if (item.Contains("⭐")) total += 0.10;
            
            if (item.Contains("StatTrak") && !item.Contains("🟡") && !item.Contains("👑")) total += total;
            if (item.Contains("Factory New") && !item.Contains("🟡") && !item.Contains("👑")) total += total;
            else if (item.Contains("Minimal Wear") && !item.Contains("🟡") && !item.Contains("👑")) total += total / 2;
            else if (item.Contains("Well-Worn") && !item.Contains("🟡") && !item.Contains("👑")) total -= total / 4;
            else if (item.Contains("Battle-Scarred") && !item.Contains("🟡") && !item.Contains("👑")) total -= total / 2;
        }

        CPH.SendMessage("📦 " + user + "'s Inventory Value: $" + total.ToString("F2"));
        return true;
    }
}
QUEST ACTIONS
ACTION: Quests_Command (!quests)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        
        string quests = CPH.GetGlobalVar<string>(user + "_Quests");
        string dailySpecificCase = CPH.GetGlobalVar<string>(user + "_DailySpecificCase");
        
        if (string.IsNullOrEmpty(quests))
        {
            string[] cases = { "Dreams", "Recoil", "Kilowatt", "Revolution", "Fracture", "Clutch", "Prisma", "DangerZone", "Horizon", "Chroma3", "Spectrum2", "CS20" };
            var rng = new Random();
            string randomCase = cases[rng.Next(0, cases.Length)];
            CPH.SetGlobalVar(user + "_DailySpecificCase", randomCase, true);
            CPH.SetGlobalVar(user + "_Quests", "Daily_3cases:0;Daily_specific:0;Daily_trade:0;Daily_sticker:0", true);
            quests = "Daily_3cases:0;Daily_specific:0;Daily_trade:0;Daily_sticker:0";
        }
        
        if (string.IsNullOrEmpty(dailySpecificCase))
        {
            string[] cases = { "Dreams", "Recoil", "Kilowatt", "Revolution", "Fracture", "Clutch", "Prisma", "DangerZone", "Horizon", "Chroma3", "Spectrum2", "CS20" };
            var rng = new Random();
            dailySpecificCase = cases[rng.Next(0, cases.Length)];
            CPH.SetGlobalVar(user + "_DailySpecificCase", dailySpecificCase, true);
        }
        
        string[] parts = quests.Split(';');
        int threeCases = 0, specificCase = 0, trade = 0, sticker = 0;
        
        foreach (string part in parts)
        {
            if (part.StartsWith("Daily_3cases:")) int.TryParse(part.Substring(13), out threeCases);
            if (part.StartsWith("Daily_specific:")) int.TryParse(part.Substring(15), out specificCase);
            if (part.StartsWith("Daily_trade:")) int.TryParse(part.Substring(12), out trade);
            if (part.StartsWith("Daily_sticker:")) int.TryParse(part.Substring(14), out sticker);
        }
        
        CPH.SendMessage($"📋 {user} Daily Quests | Open 3 cases ({threeCases}/3) | Open a {dailySpecificCase} case ({specificCase}/1) | Complete a trade ({trade}/1) | Apply a sticker ({sticker}/1) | Type !claim when done!");
        return true;
    }
}
ACTION: Claim_Command (!claim)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string quests = CPH.GetGlobalVar<string>(user + "_Quests");
        
        if (string.IsNullOrEmpty(quests))
        {
            CPH.SendMessage(user + " No active quests! Type !quests to start!");
            return false;
        }
        
        string[] parts = quests.Split(';');
        int threeCases = 0, specificCase = 0, trade = 0, sticker = 0;
        
        foreach (string part in parts)
        {
            if (part.StartsWith("Daily_3cases:")) threeCases = int.Parse(part.Substring(13));
            if (part.StartsWith("Daily_specific:")) specificCase = int.Parse(part.Substring(15));
            if (part.StartsWith("Daily_trade:")) trade = int.Parse(part.Substring(12));
            if (part.StartsWith("Daily_sticker:")) sticker = int.Parse(part.Substring(14));
        }
        
        double reward = 0;
        string completedQuests = "";
        
        if (threeCases >= 3) { reward += 5.00; completedQuests += "✓ Open 3 cases "; }
        if (specificCase >= 1) { reward += 3.00; completedQuests += "✓ Specific case "; }
        if (trade >= 1) { reward += 2.00; completedQuests += "✓ Trade "; }
        if (sticker >= 1) { reward += 2.00; completedQuests += "✓ Apply sticker "; }
        
        if (reward == 0)
        {
            CPH.SendMessage(user + " No quests completed yet! Type !quests to see your progress.");
            return false;
        }
        
        string[] cases = { "Dreams", "Recoil", "Kilowatt", "Revolution", "Fracture", "Clutch", "Prisma", "DangerZone", "Horizon", "Chroma3", "Spectrum2", "CS20" };
        var rng = new Random();
        string randomCase = cases[rng.Next(0, cases.Length)];
        CPH.SetGlobalVar(user + "_DailySpecificCase", randomCase, true);
        CPH.SetGlobalVar(user + "_Quests", "Daily_3cases:0;Daily_specific:0;Daily_trade:0;Daily_sticker:0", true);
        
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        balance += reward;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        CPH.SendMessage("🎉 " + user + " claimed quest rewards! Completed: " + completedQuests + " | Earned: $" + reward.ToString("F2") + " | New balance: $" + balance.ToString("F2"));
        return true;
    }
}
LEADERBOARD ACTIONS
ACTION: TopStreaks_Command (!topstreaks)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        if (string.IsNullOrEmpty(allUsers)) { CPH.SendMessage("No users yet!"); return false; }

        string[] users = allUsers.Split('§');
        var streaks = new List<KeyValuePair<string, int>>();
        
        foreach (string u in users)
        {
            int streak = CPH.GetGlobalVar<int>(u + "_OpenStreak");
            if (streak > 0) streaks.Add(new KeyValuePair<string, int>(u, streak));
        }

        streaks.Sort((a, b) => b.Value.CompareTo(a.Value));
        if (streaks.Count == 0) { CPH.SendMessage("No streaks yet!"); return true; }

        string msg = "🔥 Longest Open Streaks — ";
        int count = Math.Min(5, streaks.Count);
        for (int i = 0; i < count; i++)
        {
            msg += (i + 1) + ". " + streaks[i].Key + " x" + streaks[i].Value;
            if (i < count - 1) msg += " | ";
        }
        CPH.SendMessage(msg);
        return true;
    }
}
ACTION: TopMoney_Command (!topmoney)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        if (string.IsNullOrEmpty(allUsers)) { CPH.SendMessage("No users yet!"); return false; }

        string[] users = allUsers.Split('§');
        var balances = new List<KeyValuePair<string, double>>();

        foreach (string u in users)
        {
            double bal = CPH.GetGlobalVar<double>(u + "_Balance");
            balances.Add(new KeyValuePair<string, double>(u, bal));
        }

        balances.Sort((a, b) => b.Value.CompareTo(a.Value));
        string msg = "💰 Top Money — ";
        int count = Math.Min(5, balances.Count);
        for (int i = 0; i < count; i++)
        {
            msg += (i + 1) + ". " + balances[i].Key + " $" + balances[i].Value.ToString("F2");
            if (i < count - 1) msg += " | ";
        }
        CPH.SendMessage(msg);
        return true;
    }
}
ACTION: TopInventory_Command (!topinventory)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        if (string.IsNullOrEmpty(allUsers)) { CPH.SendMessage("No users yet!"); return false; }

        string[] users = allUsers.Split('§');
        var values = new List<KeyValuePair<string, double>>();

        foreach (string u in users)
        {
            string items = CPH.GetGlobalVar<string>(u + "_Items");
            double total = 0;
            if (!string.IsNullOrEmpty(items))
            {
                foreach (string item in items.Split(';'))
                {
                    if (item.Contains("🟡"))
                    {
                        if (item.Contains("Mythic")) total += 2500.00;
                        else if (item.Contains("Legendary")) total += 1000.00;
                        else if (item.Contains("Epic")) total += 600.00;
                        else if (item.Contains("Rare")) total += 300.00;
                        else total += 150.00;
                    }
                    else if (item.Contains("🔴")) total += 20.00;
                    else if (item.Contains("💗")) total += 4.00;
                    else if (item.Contains("🟣")) total += 1.00;
                    else if (item.Contains("🔷")) total += 0.25;
                    else if (item.Contains("👑"))
                    {
                        if (item.Contains("Legendary")) total += 300.00;
                        else if (item.Contains("Epic")) total += 150.00;
                        else if (item.Contains("Rare")) total += 75.00;
                        else total += 30.00;
                    }
                    else if (item.Contains("🔥")) total += 10.00;
                    else if (item.Contains("💫")) total += 2.00;
                    else if (item.Contains("✨")) total += 0.50;
                    else if (item.Contains("⭐")) total += 0.10;
                    
                    if (item.Contains("StatTrak") && !item.Contains("🟡") && !item.Contains("👑")) total += total;
                    if (item.Contains("Factory New") && !item.Contains("🟡") && !item.Contains("👑")) total += total;
                    else if (item.Contains("Minimal Wear") && !item.Contains("🟡") && !item.Contains("👑")) total += total / 2;
                    else if (item.Contains("Well-Worn") && !item.Contains("🟡") && !item.Contains("👑")) total -= total / 4;
                    else if (item.Contains("Battle-Scarred") && !item.Contains("🟡") && !item.Contains("👑")) total -= total / 2;
                }
            }
            values.Add(new KeyValuePair<string, double>(u, total));
        }

        values.Sort((a, b) => b.Value.CompareTo(a.Value));
        string msg = "🏆 Highest Inventory Value — ";
        int count = Math.Min(5, values.Count);
        for (int i = 0; i < count; i++)
        {
            msg += (i + 1) + ". " + values[i].Key + " $" + values[i].Value.ToString("F2");
            if (i < count - 1) msg += " | ";
        }
        CPH.SendMessage(msg);
        return true;
    }
}
ACTION: TopGolds_Command (!topgolds)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        if (string.IsNullOrEmpty(allUsers)) { CPH.SendMessage("No users yet!"); return false; }

        string[] users = allUsers.Split('§');
        var golds = new List<KeyValuePair<string, int>>();
        foreach (string u in users)
        {
            int g = CPH.GetGlobalVar<int>(u + "_Golds");
            if (g > 0) golds.Add(new KeyValuePair<string, int>(u, g));
        }

        golds.Sort((a, b) => b.Value.CompareTo(a.Value));
        if (golds.Count == 0) { CPH.SendMessage("Nobody has pulled a gold yet!"); return true; }

        string msg = "🥇 Gold Pull Leaders — ";
        int count = Math.Min(5, golds.Count);
        for (int i = 0; i < count; i++)
        {
            msg += (i + 1) + ". " + golds[i].Key + " x" + golds[i].Value;
            if (i < count - 1) msg += " | ";
        }
        CPH.SendMessage(msg);
        return true;
    }
}
ACTION: TopOpens_Command (!topopens)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        if (string.IsNullOrEmpty(allUsers)) { CPH.SendMessage("No users yet!"); return false; }

        string[] users = allUsers.Split('§');
        var opens = new List<KeyValuePair<string, int>>();
        foreach (string u in users)
        {
            int o = CPH.GetGlobalVar<int>(u + "_TotalOpened");
            if (o > 0) opens.Add(new KeyValuePair<string, int>(u, o));
        }

        opens.Sort((a, b) => b.Value.CompareTo(a.Value));
        string msg = "🏆 Most Cases Opened — ";
        int count = Math.Min(5, opens.Count);
        for (int i = 0; i < count; i++)
        {
            msg += (i + 1) + ". " + opens[i].Key + " x" + opens[i].Value;
            if (i < count - 1) msg += " | ";
        }
        CPH.SendMessage(msg);
        return true;
    }
}
COLLECTION ACTIONS
ACTION: Collections_Command (!collections)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string achievements = CPH.GetGlobalVar<string>(user + "_Achievements");
        
        int dreams = CPH.GetGlobalVar<int>(user + "_Dreams_Opened");
        int recoil = CPH.GetGlobalVar<int>(user + "_Recoil_Opened");
        int kilowatt = CPH.GetGlobalVar<int>(user + "_Kilowatt_Opened");
        int fracture = CPH.GetGlobalVar<int>(user + "_Fracture_Opened");
        int clutch = CPH.GetGlobalVar<int>(user + "_Clutch_Opened");
        int prisma = CPH.GetGlobalVar<int>(user + "_Prisma_Opened");
        int cs20 = CPH.GetGlobalVar<int>(user + "_CS20_Opened");
        int spectrum2 = CPH.GetGlobalVar<int>(user + "_Spectrum2_Opened");
        int chroma3 = CPH.GetGlobalVar<int>(user + "_Chroma3_Opened");
        
        int classic = (dreams > 0 ? 1 : 0) + (recoil > 0 ? 1 : 0) + (kilowatt > 0 ? 1 : 0);
        int operation = (fracture > 0 ? 1 : 0) + (clutch > 0 ? 1 : 0) + (prisma > 0 ? 1 : 0);
        int legacy = (cs20 > 0 ? 1 : 0) + (spectrum2 > 0 ? 1 : 0) + (chroma3 > 0 ? 1 : 0);
        
        string classicBadge = (classic >= 3 && achievements.Contains("ClassicCollector")) ? " ✓" : "";
        string operationBadge = (operation >= 3 && achievements.Contains("OperationVeteran")) ? " ✓" : "";
        string legacyBadge = (legacy >= 3 && achievements.Contains("LegacyKeeper")) ? " ✓" : "";
        string masterBadge = (classic + operation + legacy >= 9 && achievements.Contains("CaseMaster")) ? " ✓" : "";
        
        CPH.SendMessage($"📚 {user} Collections | Classic:{classic}/3{classicBadge} (Dreams, Recoil, Kilowatt) ➜ $100+🏆 | Operation:{operation}/3{operationBadge} (Fracture, Clutch, Prisma) ➜ $150+🏆 | Legacy:{legacy}/3{legacyBadge} (CS20, Spectrum2, Chroma3) ➜ $200+🏆 | Master:{classic + operation + legacy}/9{masterBadge} ➜ $500+🏆");
        return true;
    }
}
LOTTERY ACTIONS
ACTION: Lottery_Command (!lottery)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        double pot = CPH.GetGlobalVar<double>("Lottery_Pot");
        string tickets = CPH.GetGlobalVar<string>("Lottery_Tickets");
        
        int ticketCount = 0;
        if (!string.IsNullOrEmpty(tickets))
        {
            string[] ticketList = tickets.Split('|');
            ticketCount = ticketList.Length;
        }
        
        CPH.SendMessage("🎫 Lottery — Prize Pool: $" + pot.ToString("F2") + " | Tickets Sold: " + ticketCount + " | $10 per ticket | 80% to winner, 20% to next week | !buyticket [quantity] to enter!");
        return true;
    }
}
ACTION: BuyTicket_Command (!buyticket)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        int quantity = 1;
        string afterCommand = rawInput.Replace("!buyticket", "").Trim();
        if (!string.IsNullOrEmpty(afterCommand))
        {
            int.TryParse(afterCommand, out quantity);
        }
        quantity = Math.Min(quantity, 100);
        
        double cost = quantity * 10.00;
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        
        if (balance < cost)
        {
            CPH.SendMessage(user + " You need $" + cost.ToString("F2") + " to buy " + quantity + " ticket(s)!");
            return false;
        }
        
        balance -= cost;
        CPH.SetGlobalVar(user + "_Balance", balance, true);
        
        double pot = CPH.GetGlobalVar<double>("Lottery_Pot");
        pot += cost * 0.8;
        CPH.SetGlobalVar("Lottery_Pot", pot, true);
        
        string tickets = CPH.GetGlobalVar<string>("Lottery_Tickets");
        for (int i = 0; i < quantity; i++)
        {
            tickets = string.IsNullOrEmpty(tickets) ? user : tickets + "|" + user;
        }
        CPH.SetGlobalVar("Lottery_Tickets", tickets, true);
        
        CPH.SendMessage("🎫 " + user + " bought " + quantity + " lottery ticket(s) for $" + cost.ToString("F2") + " | Balance: $" + balance.ToString("F2") + " | Prize Pool: $" + pot.ToString("F2"));
        return true;
    }
}
ACTION: DrawLottery_Command (!drawlottery) - Mods Only
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string tickets = CPH.GetGlobalVar<string>("Lottery_Tickets");
        double pot = CPH.GetGlobalVar<double>("Lottery_Pot");
        
        if (string.IsNullOrEmpty(tickets))
        {
            CPH.SendMessage("No lottery tickets sold this week!");
            return false;
        }
        
        string[] ticketList = tickets.Split('|');
        var rng = new Random();
        string winner = ticketList[rng.Next(0, ticketList.Length)];
        
        double winnerShare = pot;
        double nextWeekRollover = 0;
        
        CPH.SetGlobalVar("Lottery_Pot", nextWeekRollover, true);
        CPH.SetGlobalVar("Lottery_Tickets", "", true);
        
        double winnerBalance = CPH.GetGlobalVar<double>(winner + "_Balance");
        winnerBalance += winnerShare;
        CPH.SetGlobalVar(winner + "_Balance", winnerBalance, true);
        
        CPH.SendMessage("🎫 LOTTERY WINNER! " + winner + " won $" + winnerShare.ToString("F2") + "! $" + nextWeekRollover.ToString("F2") + " rolls over to next week!");
        return true;
    }
}
REFERRAL ACTIONS
ACTION: Refer_Command (!refer)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string referrer = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string target = rawInput.Replace("!refer", "").Trim().Replace("@", "");
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage(referrer + " Usage: !refer @username | Refer a new user to the bot!");
            return false;
        }
        
        if (referrer.Equals(target, StringComparison.OrdinalIgnoreCase))
        {
            CPH.SendMessage(referrer + " You can't refer yourself!");
            return false;
        }
        
        int dailyReferrals = CPH.GetGlobalVar<int>(referrer + "_DailyReferrals");
        if (dailyReferrals >= 5)
        {
            CPH.SendMessage(referrer + " You've reached your daily referral limit (5 per day)!");
            return false;
        }
        
        string referredBy = CPH.GetGlobalVar<string>(target + "_ReferredBy");
        if (!string.IsNullOrEmpty(referredBy))
        {
            CPH.SendMessage(referrer + " User @" + target + " has already been referred!");
            return false;
        }
        
        CPH.SetGlobalVar(target + "_ReferredBy", referrer, true);
        
        CPH.SendMessage(referrer + " You referred @" + target + "! When they open 10 cases, you'll get $25. If they pull a Gold, you'll get $100!");
        return true;
    }
}
ACTION: Referrals_Command (!referrals)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        int referrals = CPH.GetGlobalVar<int>(user + "_Referrals");
        
        CPH.SendMessage("📎 " + user + " has referred " + referrals + " user(s)! | Refer new users to earn $5 when they use !daily, $25 when they open 10 cases, and $100 if they pull a Gold!");
        return true;
    }
}
GIVEAWAY ACTIONS
ACTION: Giveaway_Command (!giveaway) - Broadcaster Only
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput.Replace("!giveaway", "").Trim();
        double amount;
        if (!double.TryParse(afterCommand, out amount) || amount <= 0)
        {
            CPH.SendMessage("Usage: !giveaway [amount] | Example: !giveaway 100");
            return false;
        }
        
        bool active = CPH.GetGlobalVar<bool>("Giveaway_Active");
        if (active)
        {
            CPH.SendMessage("A giveaway is already active!");
            return false;
        }
        
        CPH.SetGlobalVar("Giveaway_Active", true, true);
        CPH.SetGlobalVar("Giveaway_Amount", amount, true);
        CPH.SetGlobalVar("Giveaway_Entries", "", true);
        CPH.SetGlobalVar("Giveaway_StartTime", DateTime.UtcNow.ToString(), true);
        
        CPH.SendMessage("🎉 GIVEAWAY STARTED! $" + amount.ToString("F2") + " prize! Type !enter to enter! Drawing in 2 minutes!");
        
        return true;
    }
}
ACTION: Enter_Command (!enter)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        
        bool active = CPH.GetGlobalVar<bool>("Giveaway_Active");
        if (!active)
        {
            CPH.SendMessage(user + " No active giveaway! Watch for !giveaway from the broadcaster!");
            return false;
        }
        
        string entries = CPH.GetGlobalVar<string>("Giveaway_Entries");
        if (!string.IsNullOrEmpty(entries) && entries.Contains(user))
        {
            CPH.SendMessage(user + " You're already entered!");
            return false;
        }
        
        entries = string.IsNullOrEmpty(entries) ? user : entries + ";" + user;
        CPH.SetGlobalVar("Giveaway_Entries", entries, true);
        
        CPH.SendMessage(user + " entered the giveaway! Good luck!");
        return true;
    }
}
ACTION: DrawGiveaway_Command (!drawgiveaway)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        bool active = CPH.GetGlobalVar<bool>("Giveaway_Active");
        if (!active)
        {
            return false;
        }
        
        string entries = CPH.GetGlobalVar<string>("Giveaway_Entries");
        double amount = CPH.GetGlobalVar<double>("Giveaway_Amount");
        
        if (string.IsNullOrEmpty(entries))
        {
            CPH.SetGlobalVar("Giveaway_Active", false, true);
            return false;
        }
        
        string[] entryList = entries.Split(';');
        var rng = new Random();
        string winner = entryList[rng.Next(0, entryList.Length)];
        
        double winnerBalance = CPH.GetGlobalVar<double>(winner + "_Balance");
        winnerBalance += amount;
        CPH.SetGlobalVar(winner + "_Balance", winnerBalance, true);
        
        CPH.SetGlobalVar("Giveaway_Active", false, true);
        
        CPH.SendMessage("🎉 GIVEAWAY WINNER! " + winner + " won $" + amount.ToString("F2") + "!");
        return true;
    }
}
JACKPOT ACTIONS
ACTION: Jackpot_Command (!jackpot)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string items = CPH.GetGlobalVar<string>(user + "_Items");

        if (string.IsNullOrEmpty(items))
        {
            CPH.SendMessage($"🎰 {user} Your inventory is empty! Open some cases first with !setcase then !case");
            return false;
        }

        string[] itemList = items.Split(';');
        
        // Find all weapons (exclude stickers)
        List<string> weaponItems = new List<string>();
        foreach (string item in itemList)
        {
            if (item.Contains("🔷") || item.Contains("🟣") || item.Contains("💗") || item.Contains("🔴") || item.Contains("🟡"))
            {
                weaponItems.Add(item);
            }
        }
        
        if (weaponItems.Count == 0)
        {
            CPH.SendMessage($"🎰 {user} You have no weapons to enter! Weapons have emojis 🔷🟣💗🔴🟡. Type !inv to see your inventory.");
            return false;
        }

        var rng = new Random();
        int index = rng.Next(0, weaponItems.Count);
        string contributed = weaponItems[index];
        
        // Calculate tickets based on item value
        double itemValue = GetItemValue(contributed);
        int tickets = (int)(itemValue / 0.25);
        if (tickets < 1) tickets = 1;
        
        // Remove the item from inventory
        var newList = new List<string>(itemList);
        newList.Remove(contributed);
        CPH.SetGlobalVar(user + "_Items", string.Join(";", newList), true);

        // Add to jackpot pot
        string pot = CPH.GetGlobalVar<string>("JackpotPot");
        string entries = CPH.GetGlobalVar<string>("JackpotEntries");

        string newEntry = user + "~" + contributed + "~" + tickets;
        pot = string.IsNullOrEmpty(pot) ? newEntry : pot + "§" + newEntry;

        string currentEntries = string.IsNullOrEmpty(entries) ? "" : entries;
        for (int i = 0; i < tickets; i++)
            currentEntries += (string.IsNullOrEmpty(currentEntries) ? "" : "§") + user;

        CPH.SetGlobalVar("JackpotPot", pot, true);
        CPH.SetGlobalVar("JackpotEntries", currentEntries, true);
        
        // Track last entry time for auto-draw
        CPH.SetGlobalVar("JackpotLastEntry", DateTime.UtcNow.ToString(), true);

        // Calculate current pot value for display
        string[] potEntries = pot.Split('§');
        double potValue = 0;
        foreach (string entry in potEntries)
        {
            string[] parts = entry.Split('~');
            if (parts.Length >= 2)
            {
                potValue += GetItemValue(parts[1]);
            }
        }

        CPH.SendMessage($"🎰 {user} entered jackpot with {contributed} ({tickets} tickets) | Pot: ${potValue:F2}");
        return true;
    }
    
    private double GetItemValue(string item)
    {
        if (item.Contains("🟡"))
        {
            if (item.Contains("Mythic")) return 2500.00;
            if (item.Contains("Legendary")) return 1000.00;
            if (item.Contains("Epic")) return 600.00;
            if (item.Contains("Rare")) return 300.00;
            return 150.00;
        }
        if (item.Contains("🔴")) return 20.00;
        if (item.Contains("💗")) return 4.00;
        if (item.Contains("🟣")) return 1.00;
        if (item.Contains("🔷")) return 0.25;
        return 0.10;
    }
}
ACTION: JackpotInfo_Command (!jackpotinfo)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string pot = CPH.GetGlobalVar<string>("JackpotPot");
        string entries = CPH.GetGlobalVar<string>("JackpotEntries");

        if (string.IsNullOrEmpty(pot))
        {
            CPH.SendMessage("🎰 No active jackpot! Type !jackpot to enter with a random weapon.");
            return true;
        }

        string[] potEntries = pot.Split(new[] { '§' }, StringSplitOptions.RemoveEmptyEntries);
        var players = new List<string>();
        double potValue = 0;

        foreach (string entry in potEntries)
        {
            string[] parts = entry.Split('~');
            if (parts.Length >= 2)
            {
                string player = parts[0];
                string item = parts[1];
                
                if (!players.Contains(player)) players.Add(player);
                potValue += GetItemValue(item);
            }
        }

        int tickets = 0;
        if (!string.IsNullOrEmpty(entries))
        {
            tickets = entries.Split(new[] { '§' }, StringSplitOptions.RemoveEmptyEntries).Length;
        }
        
        string playerList = string.Join(", ", players);
        CPH.SendMessage($"🎰 Current Jackpot — Players: {players.Count} ({playerList}) | Items: {potEntries.Length} | Pot: ${potValue:F2} | Tickets: {tickets} | Auto-draw after 2 minutes of no entries!");
        return true;
    }
    
    private double GetItemValue(string item)
    {
        if (item.Contains("🟡"))
        {
            if (item.Contains("Mythic")) return 2500.00;
            if (item.Contains("Legendary")) return 1000.00;
            if (item.Contains("Epic")) return 600.00;
            if (item.Contains("Rare")) return 300.00;
            return 150.00;
        }
        if (item.Contains("🔴")) return 20.00;
        if (item.Contains("💗")) return 4.00;
        if (item.Contains("🟣")) return 1.00;
        if (item.Contains("🔷")) return 0.25;
        return 0.10;
    }
}
ACTION: Jackpot_Draw (!jackpotdraw) - With Auto-Draw Check
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        // Check if enough time has passed for auto-draw
        string lastEntry = CPH.GetGlobalVar<string>("JackpotLastEntry");
        if (!string.IsNullOrEmpty(lastEntry))
        {
            DateTime lastEntryTime = DateTime.Parse(lastEntry);
            if ((DateTime.UtcNow - lastEntryTime).TotalSeconds < 120)
            {
                // Less than 2 minutes since last entry, don't draw yet
                return false;
            }
        }
        
        string entries = CPH.GetGlobalVar<string>("JackpotEntries");
        string pot = CPH.GetGlobalVar<string>("JackpotPot");

        // Silent check - no message
        if (string.IsNullOrEmpty(entries))
        {
            return false;
        }

        string[] entryList = entries.Split(new[] { '§' }, StringSplitOptions.RemoveEmptyEntries);
        if (entryList.Length == 0)
        {
            return false;
        }

        var rng = new Random();
        string winner = entryList[rng.Next(0, entryList.Length)];

        string[] potEntries = pot.Split(new[] { '§' }, StringSplitOptions.RemoveEmptyEntries);
        var winnings = new System.Collections.Generic.List<string>();
        double potValue = 0;

        foreach (string entry in potEntries)
        {
            string[] parts = entry.Split('~');
            if (parts.Length >= 2)
            {
                winnings.Add(parts[1]);
                potValue += GetItemValue(parts[1]);
            }
        }

        string winnerItems = CPH.GetGlobalVar<string>(winner + "_Items");
        string allWinnings = string.Join(";", winnings);
        
        if (string.IsNullOrEmpty(winnerItems))
            CPH.SetGlobalVar(winner + "_Items", allWinnings, true);
        else
            CPH.SetGlobalVar(winner + "_Items", winnerItems + ";" + allWinnings, true);
        
        int jackpotWins = CPH.GetGlobalVar<int>(winner + "_JackpotWins");
        jackpotWins++;
        CPH.SetGlobalVar(winner + "_JackpotWins", jackpotWins, true);

        CPH.SetGlobalVar("JackpotPot", "", true);
        CPH.SetGlobalVar("JackpotEntries", "", true);
        CPH.SetGlobalVar("JackpotLastEntry", "", true);

        CPH.SendMessage($"🎰 JACKPOT WINNER! {winner} won {winnings.Count} items worth ${potValue:F2}!");
        return true;
    }
    
    private double GetItemValue(string item)
    {
        if (item.Contains("🟡"))
        {
            if (item.Contains("Mythic")) return 2500.00;
            if (item.Contains("Legendary")) return 1000.00;
            if (item.Contains("Epic")) return 600.00;
            if (item.Contains("Rare")) return 300.00;
            return 150.00;
        }
        if (item.Contains("🔴")) return 20.00;
        if (item.Contains("💗")) return 4.00;
        if (item.Contains("🟣")) return 1.00;
        if (item.Contains("🔷")) return 0.25;
        return 0.10;
    }
}
TRADING ACTIONS
ACTION: Trade_Command (!trade)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string sender = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!trade"))
        {
            int tradeIndex = afterCommand.IndexOf("!trade");
            afterCommand = afterCommand.Substring(tradeIndex + 6).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage(sender + " Usage: !trade @username [item number]\n\nUse !inv or !stickers to see your items numbered\n\nExample: !trade @user 3");
            return false;
        }
        
        string target = "";
        int itemIndex = -1;
        
        foreach (string part in parts)
        {
            int testNumber;
            if (int.TryParse(part, out testNumber))
            {
                itemIndex = testNumber;
            }
            else
            {
                string cleanPart = part.Replace("@", "").Trim();
                if (!string.IsNullOrEmpty(cleanPart)) target = cleanPart;
            }
        }
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage(sender + " Please specify a username!");
            return false;
        }
        
        if (itemIndex == -1)
        {
            CPH.SendMessage(sender + " Please specify an item number!");
            return false;
        }
        
        string senderItems = CPH.GetGlobalVar<string>(sender + "_Items");
        if (string.IsNullOrEmpty(senderItems))
        {
            CPH.SendMessage(sender + " Your inventory is empty!");
            return false;
        }
        
        string[] itemList = senderItems.Split(';');
        
        List<string> visibleItems = new List<string>();
        foreach (string item in itemList)
        {
            if (item.Contains("🔷") || item.Contains("🟣") || item.Contains("💗") || item.Contains("🔴") || item.Contains("🟡") ||
                item.Contains("⭐") || item.Contains("✨") || item.Contains("💫") || item.Contains("🔥") || item.Contains("👑"))
            {
                visibleItems.Add(item);
            }
        }
        
        if (itemIndex < 1 || itemIndex > visibleItems.Count)
        {
            CPH.SendMessage(sender + " Invalid item number! You have " + visibleItems.Count + " tradeable items.");
            return false;
        }
        
        string tradeItem = visibleItems[itemIndex - 1];
        
        double targetBalance = CPH.GetGlobalVar<double>(target + "_Balance");
        if (targetBalance == 0 && CPH.GetGlobalVar<string>(target + "_Items") == null)
        {
            CPH.SendMessage(sender + " User @" + target + " hasn't played the bot yet!\n\nThey need to type !daily first to create their account.");
            return false;
        }
        
        if (sender.Equals(target, StringComparison.OrdinalIgnoreCase))
        {
            CPH.SendMessage(sender + " You can't trade with yourself!\n\nIf you want to get rid of items, use !sell or !sellall.");
            return false;
        }
        
        int actualIndex = -1;
        for (int i = 0; i < itemList.Length; i++)
        {
            if (itemList[i] == tradeItem)
            {
                actualIndex = i;
                break;
            }
        }
        
        // Update quest progress for trade
        UpdateQuestProgress(sender, "trade");
        
        CPH.SetGlobalVar("Trade_Sender", sender, true);
        CPH.SetGlobalVar("Trade_Target", target, true);
        CPH.SetGlobalVar("Trade_Item", tradeItem, true);
        CPH.SetGlobalVar("Trade_ItemIndex", actualIndex + 1, true);
        CPH.SetGlobalVar("Trade_Active", "true", true);
        
        CPH.SendMessage("📦 " + sender + " offered to trade " + tradeItem + " to @" + target + " | @" + target + " type !accepttrade to accept or !denytrade to deny (30 seconds)");
        
        return true;
    }
    
    private void UpdateQuestProgress(string user, string questType)
    {
        string quests = CPH.GetGlobalVar<string>(user + "_Quests");
        if (string.IsNullOrEmpty(quests))
        {
            quests = "Daily_3cases:0;Daily_specific:0;Daily_trade:0;Daily_sticker:0";
        }
        
        if (questType == "trade")
        {
            string[] parts = quests.Split(';');
            for (int i = 0; i < parts.Length; i++)
            {
                if (parts[i].StartsWith("Daily_trade:"))
                {
                    int count = int.Parse(parts[i].Substring(12));
                    count++;
                    parts[i] = "Daily_trade:" + count;
                    break;
                }
            }
            quests = string.Join(";", parts);
        }
        
        CPH.SetGlobalVar(user + "_Quests", quests, true);
    }
}
ACTION: AcceptTrade_Command (!accepttrade)
csharp
using System;
using System.Collections.Generic;

public class CPHInline
{
    public bool Execute()
    {
        string target = args["userName"].ToString();
        string tradeActive = CPH.GetGlobalVar<string>("Trade_Active");
        
        if (tradeActive != "true")
        {
            CPH.SendMessage(target + " No active trade offer!");
            return false;
        }
        
        string sender = CPH.GetGlobalVar<string>("Trade_Sender");
        string tradeTarget = CPH.GetGlobalVar<string>("Trade_Target");
        
        if (!target.Equals(tradeTarget, StringComparison.OrdinalIgnoreCase))
        {
            CPH.SendMessage(target + " This trade offer is not for you!");
            return false;
        }
        
        string tradeItem = CPH.GetGlobalVar<string>("Trade_Item");
        int itemIndex = CPH.GetGlobalVar<int>("Trade_ItemIndex");
        
        string senderItems = CPH.GetGlobalVar<string>(sender + "_Items");
        string targetItems = CPH.GetGlobalVar<string>(target + "_Items");
        
        if (string.IsNullOrEmpty(senderItems))
        {
            CPH.SendMessage("Trade cancelled - " + sender + " no longer has that item!");
            CPH.SetGlobalVar("Trade_Active", "false", true);
            return false;
        }
        
        string[] senderList = senderItems.Split(';');
        if (itemIndex < 1 || itemIndex > senderList.Length)
        {
            CPH.SendMessage("Trade cancelled - item no longer exists!");
            CPH.SetGlobalVar("Trade_Active", "false", true);
            return false;
        }
        
        var newSenderList = new List<string>(senderList);
        newSenderList.RemoveAt(itemIndex - 1);
        CPH.SetGlobalVar(sender + "_Items", string.Join(";", newSenderList), true);
        
        string newTargetItems = string.IsNullOrEmpty(targetItems) ? tradeItem : targetItems + ";" + tradeItem;
        CPH.SetGlobalVar(target + "_Items", newTargetItems, true);
        
        CPH.SetGlobalVar("Trade_Active", "false", true);
        CPH.SendMessage("✅ Trade completed! " + sender + " gave " + tradeItem + " to " + target + "!");
        return true;
    }
}
ACTION: DenyTrade_Command (!denytrade)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string tradeActive = CPH.GetGlobalVar<string>("Trade_Active");
        
        if (tradeActive != "true")
        {
            CPH.SendMessage(user + " No active trade offer!");
            return false;
        }
        
        string sender = CPH.GetGlobalVar<string>("Trade_Sender");
        string target = CPH.GetGlobalVar<string>("Trade_Target");
        
        if (!user.Equals(target, StringComparison.OrdinalIgnoreCase) && !user.Equals(sender, StringComparison.OrdinalIgnoreCase))
        {
            CPH.SendMessage(user + " This trade offer doesn't involve you!");
            return false;
        }
        
        CPH.SetGlobalVar("Trade_Active", "false", true);
        CPH.SendMessage("❌ Trade cancelled by " + user + "!");
        return true;
    }
}
STATS ACTIONS
ACTION: MyStats_Command (!mystats)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        int total = CPH.GetGlobalVar<int>(user + "_TotalOpened");
        int golds = CPH.GetGlobalVar<int>(user + "_Golds");
        double balance = CPH.GetGlobalVar<double>(user + "_Balance");
        int streak = CPH.GetGlobalVar<int>(user + "_OpenStreak");
        int tradeups = CPH.GetGlobalVar<int>(user + "_TradeupCount");
        int jackpotWins = CPH.GetGlobalVar<int>(user + "_JackpotWins");
        int referrals = CPH.GetGlobalVar<int>(user + "_Referrals");
        
        int dreams = CPH.GetGlobalVar<int>(user + "_Dreams_Opened");
        int recoil = CPH.GetGlobalVar<int>(user + "_Recoil_Opened");
        int kilowatt = CPH.GetGlobalVar<int>(user + "_Kilowatt_Opened");
        int revolution = CPH.GetGlobalVar<int>(user + "_Revolution_Opened");
        int fracture = CPH.GetGlobalVar<int>(user + "_Fracture_Opened");
        int clutch = CPH.GetGlobalVar<int>(user + "_Clutch_Opened");
        int prisma = CPH.GetGlobalVar<int>(user + "_Prisma_Opened");
        int dangerzone = CPH.GetGlobalVar<int>(user + "_DangerZone_Opened");
        int horizon = CPH.GetGlobalVar<int>(user + "_Horizon_Opened");
        int chroma3 = CPH.GetGlobalVar<int>(user + "_Chroma3_Opened");
        int spectrum2 = CPH.GetGlobalVar<int>(user + "_Spectrum2_Opened");
        int cs20 = CPH.GetGlobalVar<int>(user + "_CS20_Opened");

        CPH.SendMessage("🎮 " + user + " — Total: " + total + " | 🟡 Golds: " + golds + " | 🔥 Streak: " + streak + " | 🔄 Trade-ups: " + tradeups + " | 🎰 Jackpot Wins: " + jackpotWins + " | 📎 Referrals: " + referrals + " | 💰 $" + balance.ToString("F2") + " | Cases: Dreams:" + dreams + " Recoil:" + recoil + " Kilowatt:" + kilowatt + " Revolution:" + revolution + " Fracture:" + fracture + " Clutch:" + clutch + " Prisma:" + prisma + " DZ:" + dangerzone + " Horizon:" + horizon + " Chroma3:" + chroma3 + " Spec2:" + spectrum2 + " CS20:" + cs20);
        return true;
    }
}
ACTION: Streak_Command (!streak)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        int streak = CPH.GetGlobalVar<int>(user + "_OpenStreak");
        
        string nextBonus = "";
        if (streak < 10) nextBonus = "10 opens ($25 bonus)";
        else if (streak < 25) nextBonus = "25 opens ($75 bonus)";
        else if (streak < 50) nextBonus = "50 opens ($250 bonus)";
        else if (streak < 100) nextBonus = "100 opens ($1000 bonus)";
        else nextBonus = "MAX! Keep going!";
        
        int currentBonus = 0;
        if (streak >= 100) currentBonus = 1000;
        else if (streak >= 50) currentBonus = 250;
        else if (streak >= 25) currentBonus = 75;
        else if (streak >= 10) currentBonus = 25;
        
        CPH.SendMessage("🔥 " + user + " current open streak: " + streak + " | Next bonus: " + nextBonus + " | Last bonus earned: $" + currentBonus);
        return true;
    }
}
ACTION: Badges_Command (!badges)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string achievements = CPH.GetGlobalVar<string>(user + "_Achievements");
        
        if (string.IsNullOrEmpty(achievements))
        {
            CPH.SendMessage("🏆 " + user + " has no badges yet! Open cases, trade up, or win jackpots to earn them!");
            return true;
        }
        
        string badgeList = "";
        int badgeCount = 0;
        
        if (achievements.Contains("FirstGold")) { badgeList += "🥇 First Gold | "; badgeCount++; }
        if (achievements.Contains("TradeUpMaster")) { badgeList += "🔄 Trade Up Master | "; badgeCount++; }
        if (achievements.Contains("JackpotWinner")) { badgeList += "🎰 Jackpot Winner | "; badgeCount++; }
        if (achievements.Contains("CaseCollector")) { badgeList += "📦 Case Collector | "; badgeCount++; }
        
        badgeList = badgeList.TrimEnd(' ', '|');
        CPH.SendMessage("🏆 " + user + " Badges (" + badgeCount + "/4): " + badgeList);
        return true;
    }
}
ACTION: Odds_Command (!odds)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        CPH.SendMessage("🎲 Case Odds — 🟡 Gold: 2.6% | 🔴 Red: 2.5% | 💗 Pink: 2.5% | 🟣 Purple: 5% | 🔷 Blue: 87.4% | Gold Tiers: Common(50%), Rare(30%), Epic(15%), Legendary(4.5%), Mythic(0.5%) | StatTrak™: 10% (2x value) | Conditions: FN(5% 2x), MW(15% 1.5x), FT(50% 1x), WW(20% 0.75x), BS(10% 0.5x) | Sticker Odds: 👑:2.6% 🔥:2.5% 💫:2.5% ✨:5% ⭐:87.4% | Gold Sticker Tiers: Common(50%), Rare(30%), Epic(15%), Legendary(5%)");
        return true;
    }
}
ACTION: Stats_Command (!stats) - Streamer Dashboard
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        string period = rawInput.Replace("!stats", "").Trim().ToLower();
        
        int dailyOpens = CPH.GetGlobalVar<int>("Stats_DailyOpens");
        int weeklyOpens = CPH.GetGlobalVar<int>("Stats_WeeklyOpens");
        int monthlyOpens = CPH.GetGlobalVar<int>("Stats_MonthlyOpens");
        
        int dreams = CPH.GetGlobalVar<int>("Dreams_Opened");
        int recoil = CPH.GetGlobalVar<int>("Recoil_Opened");
        int kilowatt = CPH.GetGlobalVar<int>("Kilowatt_Opened");
        
        string allUsers = CPH.GetGlobalVar<string>("AllUsers");
        int userCount = string.IsNullOrEmpty(allUsers) ? 0 : allUsers.Split('§').Length;
        
        if (period == "daily")
        {
            CPH.SendMessage($"📊 Daily Stats — Total Opens: {dailyOpens} | Type !stats weekly for more!");
        }
        else if (period == "weekly")
        {
            CPH.SendMessage($"📊 Weekly Stats — Total Opens: {weeklyOpens} | Type !stats monthly for more!");
        }
        else if (period == "monthly")
        {
            CPH.SendMessage($"📊 Monthly Stats — Total Opens: {monthlyOpens} | Type !stats cases for case popularity!");
        }
        else if (period == "cases")
        {
            CPH.SendMessage($"📊 Most Popular Cases — Dreams: {dreams} | Recoil: {recoil} | Kilowatt: {kilowatt} | Type !stats users for active users!");
        }
        else if (period == "users")
        {
            CPH.SendMessage($"📊 Active Users — Total: {userCount} | Type !stats for available options!");
        }
        else
        {
            CPH.SendMessage($"📊 Stats Dashboard — Daily: {dailyOpens} opens | Weekly: {weeklyOpens} | Monthly: {monthlyOpens} | Active Users: {userCount} | Type !stats daily/weekly/monthly/cases/users for details");
        }
        
        return true;
    }
}
ADMIN ACTIONS
ACTION: GiveBalance_Command (!givebalance)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!givebalance"))
        {
            int cmdIndex = afterCommand.IndexOf("!givebalance");
            afterCommand = afterCommand.Substring(cmdIndex + 12).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !givebalance @username amount | Example: !givebalance @user 100");
            return false;
        }
        
        string target = "";
        double amount = 0;
        
        foreach (string part in parts)
        {
            string cleanPart = part.Replace("@", "").Trim();
            double testAmount;
            if (double.TryParse(cleanPart, out testAmount))
            {
                amount = testAmount;
            }
            else if (string.IsNullOrEmpty(target))
            {
                target = cleanPart;
            }
        }
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage("Please specify a username!");
            return false;
        }
        
        if (amount <= 0)
        {
            CPH.SendMessage("Please specify a valid amount!");
            return false;
        }
        
        double balance = CPH.GetGlobalVar<double>(target + "_Balance");
        balance += amount;
        CPH.SetGlobalVar(target + "_Balance", balance, true);
        CPH.SendMessage("💰 Admin gave " + target + " $" + amount.ToString("F2") + " | New balance: $" + balance.ToString("F2"));
        return true;
    }
}
ACTION: TakeBalance_Command (!takebalance)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!takebalance"))
        {
            int cmdIndex = afterCommand.IndexOf("!takebalance");
            afterCommand = afterCommand.Substring(cmdIndex + 12).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !takebalance @username amount | Example: !takebalance @user 50");
            return false;
        }
        
        string target = "";
        double amount = 0;
        
        foreach (string part in parts)
        {
            string cleanPart = part.Replace("@", "").Trim();
            double testAmount;
            if (double.TryParse(cleanPart, out testAmount))
            {
                amount = testAmount;
            }
            else if (string.IsNullOrEmpty(target))
            {
                target = cleanPart;
            }
        }
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage("Please specify a username!");
            return false;
        }
        
        if (amount <= 0)
        {
            CPH.SendMessage("Please specify a valid amount!");
            return false;
        }
        
        double balance = CPH.GetGlobalVar<double>(target + "_Balance");
        balance -= amount;
        if (balance < 0) balance = 0;
        CPH.SetGlobalVar(target + "_Balance", balance, true);
        CPH.SendMessage("💰 Admin took $" + amount.ToString("F2") + " from " + target + " | New balance: $" + balance.ToString("F2"));
        return true;
    }
}
ACTION: GiveItem_Command (!giveitem)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!giveitem"))
        {
            int cmdIndex = afterCommand.IndexOf("!giveitem");
            afterCommand = afterCommand.Substring(cmdIndex + 9).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !giveitem @username skin [case] | !giveitem @username sticker [capsule] | !giveitem @username goldsticker\n\nExamples:\n!giveitem @user skin Dreams\n!giveitem @user sticker Recoil\n!giveitem @user goldsticker");
            return false;
        }
        
        string target = parts[0].Replace("@", "").Trim();
        string itemType = parts[1].ToLower();
        string caseOrCapsule = parts.Length > 2 ? parts[2] : "Dreams";
        
        var rng = new Random();
        string itemName = "";
        
        switch (itemType)
        {
            case "skin":
                string casePrefix = caseOrCapsule.Replace(" ", "");
                string[] rarities = { "Blue", "Purple", "Pink", "Red", "Gold" };
                string rarity = rarities[rng.Next(0, rarities.Length)];
                string skinListKey = casePrefix + rarity + "List";
                string skinList = CPH.GetGlobalVar<string>(skinListKey);
                if (!string.IsNullOrEmpty(skinList))
                {
                    string[] skins = skinList.Split(';');
                    string skin = skins[rng.Next(0, skins.Length)].Trim();
                    string[] conditions = { "Factory New", "Minimal Wear", "Field-Tested", "Well-Worn", "Battle-Scarred" };
                    string condition = conditions[rng.Next(0, conditions.Length)];
                    string emoji = rarity == "Blue" ? "🔷" : (rarity == "Purple" ? "🟣" : (rarity == "Pink" ? "💗" : (rarity == "Red" ? "🔴" : "🟡")));
                    itemName = $"{emoji} {skin} ({condition})";
                }
                else
                {
                    CPH.SendMessage($"Case '{caseOrCapsule}' not found. Available: Dreams, Recoil, Kilowatt, Revolution, Fracture, Clutch, Prisma, DangerZone, Horizon, Chroma3, Spectrum2, CS20");
                    return false;
                }
                break;
                
            case "sticker":
                string[] stickerRarities = { "Blue", "Purple", "Pink", "Red", "Gold" };
                string stickerRarity = stickerRarities[rng.Next(0, stickerRarities.Length)];
                string stickerListKey = caseOrCapsule + "Sticker" + stickerRarity + "List";
                string stickerList = CPH.GetGlobalVar<string>(stickerListKey);
                if (!string.IsNullOrEmpty(stickerList))
                {
                    string[] stickers = stickerList.Split(';');
                    string sticker = stickers[rng.Next(0, stickers.Length)].Trim();
                    string emoji = stickerRarity == "Blue" ? "⭐" : (stickerRarity == "Purple" ? "✨" : (stickerRarity == "Pink" ? "💫" : (stickerRarity == "Red" ? "🔥" : "👑")));
                    itemName = $"{emoji} {sticker}";
                }
                else
                {
                    CPH.SendMessage($"Capsule '{caseOrCapsule}' not found. Available: Recoil, Dreams, Championship, Legends, CS20");
                    return false;
                }
                break;
                
            case "goldsticker":
                string[] goldTiers = { "Common", "Rare", "Epic", "Legendary" };
                string goldTier = goldTiers[rng.Next(0, goldTiers.Length)];
                string[] goldStickerNames = { "Gold ESL Skull", "Gold Crown", "Gold Phoenix", "Gold Dragon", "Gold Katowice 2014", "Gold Titan", "Gold Howling Dawn", "Gold iBuyPower" };
                string goldStickerName = goldStickerNames[rng.Next(0, goldStickerNames.Length)];
                itemName = $"👑 {goldStickerName} [{goldTier}]";
                break;
                
            default:
                CPH.SendMessage("Unknown item type! Use: skin, sticker, or goldsticker");
                return false;
        }
        
        string currentItems = CPH.GetGlobalVar<string>(target + "_Items");
        string newItems = string.IsNullOrEmpty(currentItems) ? itemName : currentItems + ";" + itemName;
        CPH.SetGlobalVar(target + "_Items", newItems, true);
        
        CPH.SendMessage($"🎁 Admin gave {target} {itemName}");
        return true;
    }
}
ACTION: ResetUser_Command (!resetuser)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!resetuser"))
        {
            int cmdIndex = afterCommand.IndexOf("!resetuser");
            afterCommand = afterCommand.Substring(cmdIndex + 10).Trim();
        }
        
        string target = afterCommand.Replace("@", "").Trim();
        
        if (string.IsNullOrEmpty(target))
        {
            CPH.SendMessage("Usage: !resetuser @username | Example: !resetuser @user");
            return false;
        }
        
        // Core data
        CPH.SetGlobalVar(target + "_Items", "", true);
        CPH.SetGlobalVar(target + "_Balance", 0.00, true);
        CPH.SetGlobalVar(target + "_TotalOpened", 0, true);
        CPH.SetGlobalVar(target + "_Golds", 0, true);
        CPH.SetGlobalVar(target + "_OpenStreak", 0, true);
        CPH.SetGlobalVar(target + "_TradeupCount", 0, true);
        CPH.SetGlobalVar(target + "_JackpotWins", 0, true);
        CPH.SetGlobalVar(target + "_UnlockedCases", "Dreams;Recoil", true);
        CPH.SetGlobalVar(target + "_Achievements", "", true);
        
        // Capsule/sticker data
        CPH.SetGlobalVar(target + "_LastCapsule", "", true);
        
        // Quest data
        CPH.SetGlobalVar(target + "_Quests", "", true);
        CPH.SetGlobalVar(target + "_DailySpecificCase", "", true);
        
        // Referral data
        CPH.SetGlobalVar(target + "_Referrals", 0, true);
        CPH.SetGlobalVar(target + "_ReferredBy", "", true);
        CPH.SetGlobalVar(target + "_DailyReferrals", 0, true);
        
        // Applied stickers data
        CPH.SetGlobalVar(target + "_AppliedStickers", "", true);
        CPH.SetGlobalVar(target + "_TotalApplied", 0, true);
        
        // Case-specific counters
        string[] allCases = { "Dreams", "Recoil", "Kilowatt", "Revolution", "Fracture", "Clutch", "Prisma", "DangerZone", "Horizon", "Chroma3", "Spectrum2", "CS20" };
        foreach (string caseName in allCases)
        {
            string casePrefix = caseName.Replace(" ", "");
            CPH.SetGlobalVar(target + "_" + casePrefix + "_Opened", 0, true);
        }
        
        CPH.SendMessage($"🔄 Admin reset {target}'s data!");
        return true;
    }
}
ACTION: SetCasePrice_Command (!setcaseprice)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!setcaseprice"))
        {
            int cmdIndex = afterCommand.IndexOf("!setcaseprice");
            afterCommand = afterCommand.Substring(cmdIndex + 13).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !setcaseprice CaseName price | Example: !setcaseprice Dreams 2.50");
            return false;
        }
        
        string caseName = parts[0];
        double price;
        
        if (!double.TryParse(parts[1], out price))
        {
            CPH.SendMessage("Invalid price! Usage: !setcaseprice Dreams 2.50");
            return false;
        }
        
        string[] validCases = { "Dreams", "Recoil", "Kilowatt", "Revolution", "Fracture", "Clutch", "Prisma", "DangerZone", "Horizon", "Chroma3", "Spectrum2", "CS20" };
        bool isValid = false;
        foreach (string validCase in validCases)
        {
            if (caseName.Equals(validCase, StringComparison.OrdinalIgnoreCase))
            {
                caseName = validCase;
                isValid = true;
                break;
            }
        }
        
        if (!isValid)
        {
            CPH.SendMessage("Unknown case: " + caseName + " | Available: Dreams, Recoil, Kilowatt, Revolution, Fracture, Clutch, Prisma, DangerZone, Horizon, Chroma3, Spectrum2, CS20");
            return false;
        }
        
        CPH.SetGlobalVar("Price_" + caseName, price, true);
        CPH.SendMessage("💰 Admin set " + caseName + " price to $" + price.ToString("F2"));
        return true;
    }
}
ACTION: SetCapsulePrice_Command (!setcapsuleprice)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!setcapsuleprice"))
        {
            int cmdIndex = afterCommand.IndexOf("!setcapsuleprice");
            afterCommand = afterCommand.Substring(cmdIndex + 16).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !setcapsuleprice CapsuleName price | Example: !setcapsuleprice Recoil 1.00");
            return false;
        }
        
        string capsuleName = parts[0];
        double price;
        
        if (!double.TryParse(parts[1], out price))
        {
            CPH.SendMessage("Invalid price! Usage: !setcapsuleprice Recoil 1.00");
            return false;
        }
        
        string[] validCapsules = { "Recoil", "Dreams", "Championship", "Legends", "CS20" };
        bool isValid = false;
        foreach (string valid in validCapsules)
        {
            if (capsuleName.Equals(valid, StringComparison.OrdinalIgnoreCase))
            {
                capsuleName = valid;
                isValid = true;
                break;
            }
        }
        
        if (!isValid)
        {
            CPH.SendMessage("Unknown capsule: " + capsuleName + " | Available: Recoil, Dreams, Championship, Legends, CS20");
            return false;
        }
        
        CPH.SetGlobalVar("Price_" + capsuleName, price, true);
        CPH.SendMessage("💰 Admin set " + capsuleName + " capsule price to $" + price.ToString("F2"));
        return true;
    }
}
ACTION: GiveCapsule_Command (!givecapsule) - Auto-Open
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!givecapsule"))
        {
            int cmdIndex = afterCommand.IndexOf("!givecapsule");
            afterCommand = afterCommand.Substring(cmdIndex + 12).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !givecapsule @username CapsuleName | Example: !givecapsule @user Recoil");
            return false;
        }
        
        string target = parts[0].Replace("@", "").Trim();
        string capsuleName = parts[1];
        
        string[] validCapsules = { "Recoil", "Dreams", "Championship", "Legends", "CS20" };
        bool isValid = false;
        foreach (string valid in validCapsules)
        {
            if (capsuleName.Equals(valid, StringComparison.OrdinalIgnoreCase))
            {
                capsuleName = valid;
                isValid = true;
                break;
            }
        }
        
        if (!isValid)
        {
            CPH.SendMessage("Unknown capsule: " + capsuleName);
            return false;
        }
        
        CPH.SetGlobalVar("User", target, true);
        CPH.SetGlobalVar("CurrentCapsule", capsuleName, true);
        CPH.SetGlobalVar(target + "_LastCapsule", capsuleName, true);
        
        CPH.SendMessage($"🎁 Admin gave {target} a {capsuleName} capsule! Opening now...");
        System.Threading.Thread.Sleep(1000);
        CPH.RunAction("CS2_OpenCapsule");
        
        return true;
    }
}
ACTION: GiveGoldSticker_Command (!givegoldsticker)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput;
        if (afterCommand.Contains("!givegoldsticker"))
        {
            int cmdIndex = afterCommand.IndexOf("!givegoldsticker");
            afterCommand = afterCommand.Substring(cmdIndex + 16).Trim();
        }
        
        string[] parts = afterCommand.Split(new[] { ' ' }, StringSplitOptions.RemoveEmptyEntries);
        
        if (parts.Length < 2)
        {
            CPH.SendMessage("Usage: !givegoldsticker @username [common/rare/epic/legendary] | Example: !givegoldsticker @user legendary");
            return false;
        }
        
        string target = parts[0].Replace("@", "").Trim();
        string tier = parts[1].ToLower();
        
        string goldTag = "";
        double value = 0;
        
        switch (tier)
        {
            case "common": goldTag = " [Common]"; value = 30.00; break;
            case "rare": goldTag = " [Rare]"; value = 75.00; break;
            case "epic": goldTag = " [Epic]"; value = 150.00; break;
            case "legendary": goldTag = " [Legendary]"; value = 300.00; break;
            default:
                CPH.SendMessage("Invalid gold tier! Use: common, rare, epic, or legendary");
                return false;
        }
        
        string sticker = "👑 Gold Admin Sticker" + goldTag;
        
        string currentItems = CPH.GetGlobalVar<string>(target + "_Items");
        string newItems = string.IsNullOrEmpty(currentItems) ? sticker : currentItems + ";" + sticker;
        CPH.SetGlobalVar(target + "_Items", newItems, true);
        
        CPH.SendMessage($"🎁 Admin gave {target} a {tier.ToUpper()} gold sticker!");
        return true;
    }
}
HELP ACTION
ACTION: Help_Command (!help)
csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        string user = args["userName"].ToString();
        string rawInput = args["rawInput"].ToString();
        
        string afterCommand = rawInput.Replace("!help", "").Trim().ToLower();
        
        if (!string.IsNullOrEmpty(afterCommand))
        {
            return ShowCommandHelp(user, afterCommand);
        }
        
        CPH.SendMessage("🎮 " + user + " CS2 Case Bot V1.6 — Help (1/3) | Cases: !setcase, !case, !caseinfo, !gift | Stickers: !setcapsule, !opencapsule, !stickers, !apply, !giftcapsule | Economy: !balance, !daily, !inv, !sell, !sellall, !tradeup");
        
        CPH.SendMessage("🎮 " + user + " CS2 Case Bot V1.6 — Help (2/3) | Jackpot: !jackpot, !jackpotinfo | Stats: !mystats, !streak, !badges, !odds | Leaderboards: !topmoney, !topinventory, !topgolds, !topopens, !topstreaks | Collections: !collections");
        
        CPH.SendMessage("🎮 " + user + " CS2 Case Bot V1.6 — Help (3/3) | Lottery: !lottery, !buyticket | Referrals: !refer, !referrals | Giveaway: !giveaway, !enter | Admin: !givebalance, !takebalance, !giveitem, !resetuser, !setcaseprice, !setcapsuleprice | Details: Type !help [command] - Example: !help case");
        
        return true;
    }
    
    private bool ShowCommandHelp(string user, string command)
    {
        switch (command)
        {
            case "case":
                CPH.SendMessage("📦 Help: Cases | !setcase [name] - Choose case | !case - Open selected case | !caseinfo - All cases with prices | !gift @user [case] - Gift case | Example: !setcase Dreams then !case");
                break;
                
            case "capsule":
            case "sticker":
                CPH.SendMessage("📯 Help: Stickers | !setcapsule [name] - Choose capsule | !opencapsule - Open capsule | !capsules - List capsules | !stickers - View stickers | !apply # # - Apply sticker to weapon | !giftcapsule @user [name] - Gift capsule | Example: !setcapsule Recoil then !opencapsule");
                break;
                
            case "sell":
                CPH.SendMessage("💰 Help: Selling | !sell - Show numbered inventory | !sell [number] - Sell that item | !sellall - Sell everything | Selling resets your streak! | Example: !sell 3");
                break;
                
            case "tradeup":
                CPH.SendMessage("🔄 Help: Trade-Up | !tradeup blue - 10 Blue → 1 Purple | !tradeup purple - 10 Purple → 1 Pink | !tradeup pink - 10 Pink → 1 Red | !tradeup red - 5 Red → 1 Gold | !tradeup sticker [color] - Same for stickers");
                break;
                
            case "daily":
                CPH.SendMessage("🎁 Help: Daily Reward | !daily - Claim daily reward | Rewards: $10-$50,000! | 60%: $10-50 | 25%: $51-150 | 12%: $151-500 | 2%: $501-2,000 | 0.9999%: $2,001-5,000 | 0.0001%: $50,000 JACKPOT! | Once every 24 hours.");
                break;
                
            case "jackpot":
                CPH.SendMessage("🎰 Help: Jackpot | !jackpot - Enter with random weapon | !jackpotinfo - Current pot and players | Auto-draw after 2 minutes of no entries.");
                break;
                
            case "giveitem":
                CPH.SendMessage("🎁 Help: !giveitem (Admin) | Usage: !giveitem @user skin [case] | !giveitem @user sticker [capsule] | !giveitem @user goldsticker | Examples: !giveitem @user skin Dreams | !giveitem @user sticker Recoil | !giveitem @user goldsticker");
                break;
                
            case "setcaseprice":
                CPH.SendMessage("💰 Help: !setcaseprice (Admin) | Usage: !setcaseprice CaseName price | Example: !setcaseprice Dreams 2.50 | Changes the cost of opening a case.");
                break;
                
            case "setcapsuleprice":
                CPH.SendMessage("💰 Help: !setcapsuleprice (Admin) | Usage: !setcapsuleprice CapsuleName price | Example: !setcapsuleprice Recoil 1.00 | Changes the cost of opening a capsule.");
                break;
                
            case "resetuser":
                CPH.SendMessage("🔄 Help: !resetuser (Admin) | Usage: !resetuser @username | Example: !resetuser @user | Completely wipes all data for that user.");
                break;
                
            case "givebalance":
                CPH.SendMessage("💰 Help: !givebalance (Admin) | Usage: !givebalance @username amount | Example: !givebalance @user 100 | Adds money to a user's balance.");
                break;
                
            case "takebalance":
                CPH.SendMessage("💰 Help: !takebalance (Admin) | Usage: !takebalance @username amount | Example: !takebalance @user 50 | Removes money from a user's balance.");
                break;
                
            default:
                CPH.SendMessage("❌ " + user + " Unknown command: '" + command + "' | Type !help to see all commands.");
                break;
        }
        return true;
    }
}
TIMER SETUP INSTRUCTIONS
How to Create a Timer (Correct Method)
Timers are created directly from an action's Triggers tab:

Open the action (e.g., ResetDailyStats_Command)

Go to the Triggers tab

Right-click in the Triggers pane → Add Trigger → Core → Timed Actions

Click Create Timer

Fill in the settings:

Name: A descriptive name (e.g., DailyStatsReset)

Interval: Time in seconds (e.g., 86400 for 24 hours)

Repeat: ✅ Checked

Enabled: ✅ Checked

Lines: 0

Action to Run: Select the action (already selected)

Click OK twice

Timers to Create
Timer Name	Interval	Action to Run	Purpose
DailyStatsReset	86400	ResetDailyStats_Command	Reset daily stats every 24 hours
WeeklyStatsReset	604800	ResetWeeklyStats_Command	Reset weekly stats every 7 days
MonthlyStatsReset	2629746	ResetMonthlyStats_Command	Reset monthly stats every ~30 days
JackpotAutoDraw	60	Jackpot_Draw	Check for jackpot draw every minute
GiveawayAutoDraw	120	DrawGiveaway_Command	Draw giveaway after 2 minutes
Reset Action Codes
These actions do NOT need any triggers. The timers call them directly.

ResetDailyStats_Command:

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        CPH.SetGlobalVar("Stats_DailyOpens", 0, true);
        return true;
    }
}
ResetWeeklyStats_Command:

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        CPH.SetGlobalVar("Stats_WeeklyOpens", 0, true);
        return true;
    }
}
ResetMonthlyStats_Command:

csharp
using System;

public class CPHInline
{
    public bool Execute()
    {
        CPH.SetGlobalVar("Stats_MonthlyOpens", 0, true);
        return true;
    }
}
END OF V1.6 COMPLETE CODE REFERENCE

Created by Mk4gtiguy | Support on Ko-fi: https://ko-fi.com/mk4gtiguy

